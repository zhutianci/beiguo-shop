# 渠道分站（P0）部署说明

> 配套：`docs/多渠道分销-设计.md`（下称「设计」，章节号以它为准）第 15 章、`docs/多渠道分销-实施分包.md` 第 11、12 节。
> 本文只写**怎么上线、怎么验、怎么退**；为什么这样设计看设计文档。命令默认在服务器 `/opt/beiguo/beiguo-shop` 下、root 执行。
>
> **通用铁律**（与以往各次部署相同）：
> - 远程命令用 Bash + base64 传递；耗时任务一律后台跑、轮询日志，不在前台等。
> - **绝不跑 `docker system df` / `docker builder du` / `du -sh /var/lib/docker/*`**——会把 dockerd 撑爆、整机两个站一起掉（交接文档第六节第 3 条）。
> - 每一步都用阳性对照验证（「查到了期望的东西」，而不只是「没报错」）。
> - **任何时候不得用旧 commit 的 schema 跑 `db push`，也不要跑 `scripts/deploy.sh`**（它写死了 `--accept-data-loss`，会删掉整本渠道账）。

---

## 〇、上线分三段，各自独立、顺序不能反

| 段 | 做什么 | 对外效果 | 何时做 |
|---|---|---|---|
| **A 应用发布** | DDL → 种子 → 构建新镜像 → 只换 app | 主站照常（代码**休眠**：`CHANNELS_ENABLED` 不开，任何 Host 都按主站处理、不查 `tenants` 表）；后台多了「来源站」列、「渠道」菜单等（见第一节） | 先做；可以分批做多次，每次都走 A 段 |
| **N nginx 发布** | 按 Host 分流；lulu 指向**静态停业页** | 主站不变；公网 lulu 只看到停业页 | A 段稳定后单独一次 |
| **G 开业** | 打开应用开关 → nginx 把 lulu 改 `channel` → DRAFT 验收 → ACTIVE | lulu 开门营业 | N 段观察期内即可，站长定日子 |

为什么 N 必须在 G 之前、且 lulu 先 `closed`：应用休眠期如果 lulu 的请求进了应用，会渲染整份主站、按主站价下单、订单记到主站（设计 4.4 第 4 条禁止的「回落主站」）。停业页是纯静态文件，不进应用。

---

## 一、发布说明（主站在休眠期也能感知到的变化，逐条告诉站长）

设计 4.10 列出的可感知变化，外加各包实施时新增的几条：

1. 后台订单 / 用户 / 卡密 / 发票 / 收据 / 财务台 / 快捷回复 / 到账 / 统计多了「来源站」列与筛选（休眠期全部是「主站」）。
2. 根布局改为动态渲染：about、terms、login 等页面不再构建期预渲染，每次请求都渲染（观察内存，见第七节 M12）。
3. 新登录签发的 JWT 带 `aud=main`；**老 token 照常有效**（`JWT_LEGACY_TOKENS` 默认 accept，上线 30 天后改 `reject`）。
4. 仪表盘「总收入」口径不变（改造前就已排除「已付但已取消」的订单；上线前复核更正了本条原先「数字可能变小」的说法），只是多了按来源站拆分（休眠期只有「主站」一组，与总收入相等）。
5. 后台订单保存（改价、改状态）改成 CAS：**打开订单弹窗之后**这张单被改过（另一位管理员保存，或买家到账、自动发卡、接码往备注里追加记录等系统写入），再保存会收到 409「订单已变化，请刷新」——关掉弹窗重新打开即可（弹窗打开时会取最新状态作为基线；终审修正后不再以「列表加载时」为基线）。
6. 标已付弹窗多一个「实收金额」（主站单选填；填了才会补建收款记录）。
7. 外部订单「清空」只清主站的行（休眠期全部行都是主站，实际无变化）。
8. 卡密批量「改售价」拒绝修改渠道单的卡（休眠期没有渠道单，实际无变化）。
9. 后台把用户提权为 ADMIN 时校验他不是渠道成员（休眠期没有成员，实际无变化）。
10. **商品删除保护**（主会话 D12）：有渠道上架、订单或已发出卡密的商品只能下架、不能删除。
11. 后台菜单多了「渠道」组（渠道管理、运营概览、售后申请、对账自检；结算单在「渠道管理 → 某渠道 → 结算单」里）和「系统 → 审计日志」；休眠期这些页面是空态，属正常。「售后申请」有待处理时菜单右侧显示红色数字（后台每 60 秒与切页面时各查一次，休眠期恒为 0、不显示）。
12. 买家侧（WP2）：留言接口的返回少了几个内部字段（买家端只用 `id / sender / content / createdAt`，不受影响）；兑换页里「已停用」「未售出」的卡与「未找到该卡密」提示相同；兑换接口多了「同一 IP + 卡密前 8 位」每分钟 16 次的限频（查询与激活各算一次，约 8 张 / 分钟；上线前复核由 8 放宽到 16，避免同批卡连续兑换被误拦）。
13. 新订单显式写 `tenant_id=1`，买家备注同时写进 `buyer_remark`（主站读取端不受影响）。
14. 渠道对账 cron 每天 03:00 顺带检查一遍主站订单的快照列（L3，只读）。
15. 登录 / 注册 / 发验证码 / 找回密码 / 登出五个接口加了写接口同源校验（设计 4.6 C4，集成阶段补）：兄弟子域（如 view.bigolab.com）或外站页面发来的请求一律 403「请求来源异常，请刷新页面后重试」。主站自己的页面、App 内置浏览器、不带 Origin 的脚本调用都不受影响。
16. 超管侧同源校验不再把请求自带的 `X-Forwarded-Host` 当作本站（nginx 也已把它置空）；经 Cloudflare 隧道改写 Host 的情况仍由 `NEXT_PUBLIC_APP_URL` / `APP_URL` 兜底，站长后台操作不受影响。
17. 后台订单留言区逐条标出「渠道回复」（休眠期没有渠道单，实际无变化）。
18. 后台「用户详情 → 按站」的发票 / 收据张数改为与订单页同一套关联规则计数（订单 id、外部订单行、sourceKey 快照），存量主站票据照常计入（新列 `shop_order_id` 零回填，只按它数会显示 0）。
19. 后台「订单导入 / 外部订单」多了「来源站」列与筛选（休眠期全部是「主站」）。

---

## 二、本地门槛（每次发布前在开发机跑，全部通过才往下走）

```bash
npx tsc --noEmit                                   # 必须零错误
node scripts/check-tenant-boundary.mjs             # 边界检查（也挂在 prebuild 上）：必须「零违规」
node scripts/check-tenant-boundary.mjs --strict    # 集成收尾时：「待他包处理的已知例外」也按违规算，必须为 0
node scripts/check-tenant-boundary.mjs --doc-paths # W8-9：设计里要改造的路径都在分包第 13 节有所有者
node scripts/itest-tenant/merge-routes.mjs --check # 路由总表与实际目录一致
# 渠道测试总入口（串行；库名必须含 dev / test，否则拒绝运行）
DATABASE_URL="mysql://root:123456@localhost:3306/beiguo_dev" npm run itest:tenant
```

- `run-all` 依次跑：边界检查（含自测、`--doc-paths`）→ 路由总表核对（含自测）→ `check-tenant-math` → `check-tenant-ledger` → `itest-tenant/wp0..wp7` → `cross-tenant`（含 nginx 层，本机要有 docker）。最后一段打印每步汇总，任何一步失败即整体失败。
- **必须串行**：每个 itest 开头都会 `cleanupAll()` 清测试数据，并行跑会互相清掉夹具，表现为与改动无关的偶发失败（主会话 D10）。
- `cross-tenant.ts --strict`：把「待集成」项也算失败。发布前应当为 0（见第九节）。
- 边界检查基线（2026-09-26，在 HEAD `24f5b0f` 上跑）：扫描 504 个文件，R7=1（`api/orders/route.ts` 原地建单）、R9=3（`jwt-secret.ts` 拒绝名单、`same-origin.ts` 与 `track/view` 读 `x-forwarded-host`）、R10=1（路由总表不存在），其余 0。集成收尾后（2026-09-26）：扫描 708 个文件，`--strict` 零违规，已知例外只剩 1 条永久例外（`jwt-secret.ts` 的拒绝名单本身）。
- **堆上限实测**：Dockerfile 的 `next build` 带 `--max-old-space-size`（服务器 1.8G 内存，靠它把 V8 堆压住）。代码量变化大的版本，发布前在开发机用同一上限跑一次 `NODE_OPTIONS=--max-old-space-size=<Dockerfile 里的值> npm run build`（含 prebuild），能过再上服务器；过不了就调高 Dockerfile 里的值并在这里记一笔。

### 提交清单（`git add` 之前逐项核对；**不要 `git add -A`**）

工作区里有两百多个未跟踪文件，其中有构建 / 发布必需的，也有绝不能进仓库的：
- **必须提交**（缺了构建或发布会失败）：
  - `src/` 下全部新增与修改；`prisma/schema.prisma`；`package.json`（prebuild）；`Dockerfile`；`docker-compose.yml`；`next.config.js`；`.env.production.example`；
  - `cron/crontab`——新增 `tenant-release`（每小时解冻）与 `tenant-reconcile`（每日对账）两行。**它是已跟踪文件的修改（` M`），不是新文件**，漏了构建照样成功、A7 之前谁也发现不了；漏提交的后果是 G 之后渠道资金永不解冻、没有每日对账与 `payoutHold` 保护；
  - `scripts/` 下**所有已跟踪文件的修改**（本期是 `check-coupon.ts`、`check-same-origin.ts`、`check-transactional-mail.ts`：构建不需要，但 D6 与主站回归检查要用）；
  - `scripts/itest-tenant.routes.json`——**Docker 构建的 prebuild 要读它**，缺了规则 R10 报「路由总表缺失」，构建直接失败（它长得像测试产物，最容易漏）；
  - `scripts/check-tenant-boundary.mjs`、`scripts/check-tenant-*.ts`、`scripts/itest-tenant/**`；
  - `nginx/nginx.conf`、`nginx/closed.html`（compose 单文件 bind-mount：缺了服务器上 docker 会在该路径建一个**目录**）；
  - `scripts/ops/ddl-gate.sh`、`scripts/ops/build-with-swap.sh`（及 `.selftest.sh`）、`scripts/sql/tenant-seed.sql`、`scripts/sql/tenant-healthcheck.sql`；
  - `docs/多渠道分销-*.md`（设计、实施分包、部署说明）。
- **绝不提交**：项目根的 `pc.html`、`v2.html`（已两次误提交又回退：9395d9c、b1fd8e4）、`.env.local`、任何 `*.log`、`private/`、`uploads/` 下的测试文件。
- 提交后自查（**同时覆盖已跟踪文件的修改 ` M` 与未跟踪文件 `??`**；只查 `??` 会漏掉 `cron/crontab` 这类改动）：
  ```bash
  git status --porcelain -uall | grep -vE '^\?\? (pc|v2)\.html$'      # 必须为空：除了两个绝不提交的草稿页，工作区没有任何未提交的东西
  git status --porcelain -uall | grep -E '^( M|\?\?) (src/|scripts/|nginx/|prisma/|cron/)'   # 最低要求：必须为空
  git grep -c 'tenant-' HEAD -- cron/crontab                             # 期望 HEAD:cron/crontab:2（提交里的 crontab 确实带着两条渠道任务）
  ```
  服务器 A2 拉取后先跑边界检查（宿主机没有 node，用临时容器：`docker run --rm -v $PWD:/w:ro -w /w node:20-alpine node scripts/check-tenant-boundary.mjs`）与 `grep -c 'tenant-' cron/crontab`（期望 2）作阳性对照。

---

## 三、环境变量（`.env.production`）

compose 里一律是**裸变量名**（`- FOO`，不写 `- FOO=${FOO}`：后者在变量未定义时会给容器一个空串，反而遮蔽镜像里 `.env.production` 的真实值）。**不写 = 代码默认值 = 休眠**。

| 变量 | 上线时的值 | 说明 |
|---|---|---|
| `CHANNELS_ENABLED` | `0` | 总开关，只有 `1` 才是开。**开 1 之前 nginx 里 lulu 必须已是 `closed`**（G1） |
| `HOST_STRICT` | `0` | `1` = 严格期（未登记 Host 404）。与 nginx 严格期**同一天**切（N6） |
| `PLATFORM_HOSTS` | `bigolab.com,www.bigolab.com,app,localhost,127.0.0.1` | 主站 Host 白名单。**站长若用服务器公网 IP 进后台，开 `CHANNELS_ENABLED` 之前要把 IP 加进来**，否则 `/admin` 404（M14） |
| `CHANNEL_HOST_SUFFIX` | 不写 | 默认 `.bigolab.com`；只认一级子域 |
| `JWT_LEGACY_TOKENS` | `accept` | 上线 30 天后改 `reject`（force-recreate app 即可） |
| `TENANT_DATA_KEY` | **现生成** | 见下 |
| `PRIVATE_FILES_DIR` | 不写 | 默认 `/app/private`（compose 已挂持久卷 `private_files`） |

**`TENANT_DATA_KEY`（主会话 D8）**：渠道收款账号 / 企业微信 webhook 的加密密钥，与 `JWT_SECRET` 解耦（站长要轮换 `JWT_SECRET`，轮换后已录的收款账号不能解不开）。

```bash
cd /opt/beiguo/beiguo-shop
# 判据是「这一行存在且值非空」，不看长度：hex（64 字符）与 base64（`openssl rand -base64 32` 是 44 字符）都合法（crypto.ts parseDataKey）。
# 只在缺失时生成；已有值一律不动——哪怕看起来不对也不自动覆盖（覆盖 = 已录的收款账号全部解不开），交给人看。
if grep -q '^TENANT_DATA_KEY=.' .env.production; then
  echo "已有，别动（值是否合规见下一步应用日志 / 录入收款账号时的提示）"
elif grep -q '^TENANT_DATA_KEY=' .env.production; then
  echo "有一行 TENANT_DATA_KEY= 但值为空：停手，人工确认这台机器之前没有用过别的密钥，再手工填入一个用 openssl rand -hex 32 生成的值"
else
  cp .env.production .env.production.bak-$(date +%Y%m%d%H%M%S)
  echo "TENANT_DATA_KEY=$(openssl rand -hex 32)" >> .env.production
fi
grep -c '^TENANT_DATA_KEY=' .env.production        # 期望 1
```

- **生成一次、永远别丢别改**：改了之后已录入的收款账号全部解不开，只能逐个重新录入。把它和 `JWT_SECRET` 一起记进站长的密码保管处。
- 缺失或短于 32 字节时，录入 / 查看收款账号一律拒绝（提示「未配置数据密钥」），其余功能不受影响（fail closed）。
- `.env.production` 会被 Next standalone 打进镜像（交接文档第六节第 2 条），所以**改完 env 要重新构建**才能让镜像里那份也更新；只改 compose 传入的值 force-recreate 即可生效（运行时 env 优先）。

---

## 四、A 段：应用发布（每次都走）

### A0 准备

```bash
cd /opt/beiguo/beiguo-shop && set -a && . ./.env.production && set +a
git rev-parse --short HEAD                  # 记下来：旧版本号，回滚标签用
df -h /                                      # 可用 < 2GB 先 docker image prune -f && docker builder prune -f（不要 system df）
```
后台「发送设置」关闭营销「启用发送」（构建与重启期间不发营销邮件）。

### A1 回滚标签 + 备份

```bash
OLD=$(git rev-parse --short HEAD)
docker tag beiguo-shop-app:latest beiguo-shop-app:rollback-$OLD
docker images beiguo-shop-app --format '{{.Tag}}' | grep rollback-$OLD      # 阳性对照：标签在

F=/opt/beiguo/backups/beiguo_shop_$(date +%Y%m%d_%H%M%S).sql
docker compose --env-file .env.production exec -T db mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" \
  --single-transaction --routines --triggers --set-gtid-purged=OFF beiguo_shop > "$F"
tail -3 "$F" | grep -q "Dump completed" && echo "备份完整：$F" || echo "备份不完整，停手"
```

### A2 拉代码

```bash
git pull origin main
git log -1 --format='%h %s'
docker run --rm -v $PWD:/w:ro -w /w node:20-alpine node scripts/check-tenant-boundary.mjs   # 宿主机没有 node（2026-09-26 核实），用临时容器跑。阳性对照：「✅ 零违规」——同时证明 scripts/itest-tenant.routes.json 已随提交拉下来（缺它 R10 报「路由总表缺失」，A6 构建会失败）
ls -la nginx/closed.html scripts/ops/ddl-gate.sh scripts/ops/build-with-swap.sh scripts/sql/tenant-seed.sql   # 四个文件都在（缺一个就是提交漏了，停手）
grep -c 'tenant-' cron/crontab                      # 期望 2（tenant-release、tenant-reconcile）；0 = crontab 的修改没提交，停手（A7 才发现就晚了：镜像已换）
```

> **注意 nginx 已在磁盘上换新**：`git pull` 之后 `nginx/nginx.conf` 就是 N 段的新版本（单文件 bind-mount）。N 段之前 nginx 容器**只要因任何原因重启**（宿主机重启、dockerd 重启、A6 构建时 OOM 连带杀容器——2026-09-25 发生过），就会加载新配置：Host 分流与 `X-Forwarded-Host` 置空会提前生效，而 `closed.html` 的卷还没挂（lulu 请求 404、不进应用；主站与新配置兼容）。所以：
> - A2 之后先跑一次 N3 的 `nginx -t` 临时容器检查（只读，不影响线上），确认新配置本身能加载；
> - A6 / A7 之后若发现 nginx 容器重启过（`docker inspect -f '{{.State.StartedAt}}' beiguo-nginx` 晚于 A2 的时间），立即按 N4 的检查清单逐条核对，必要时按 N7 回滚这一个文件。

### A3 DDL 预览 + 闸门（禁止在 app 容器里执行：容器里是旧 schema）

```bash
NET=$(docker inspect -f '{{range $k, $v := .NetworkSettings.Networks}}{{$k}}{{end}}' beiguo-db)
docker run --rm --network "$NET" -v "$PWD/prisma":/work/prisma:ro -w /work \
  -e DATABASE_URL="mysql://root:$MYSQL_ROOT_PASSWORD@db:3306/beiguo_shop" \
  -e npm_config_registry=https://registry.npmmirror.com \
  -e PRISMA_ENGINES_MIRROR=https://registry.npmmirror.com/-/binary/prisma \
  node:20-alpine sh -c 'apk add --no-cache openssl >/dev/null && npx -y prisma@5.22.0 migrate diff \
    --from-url "$DATABASE_URL" --to-schema-datamodel prisma/schema.prisma --script' > /tmp/preview.sql 2>/tmp/preview.err
echo "exit=$?"; wc -c /tmp/preview.sql

# 闸门：① 0 字节 = 命令失败（不是没有变更）② 语句开头的 DROP / MODIFY / CHANGE / RENAME 必须为 0
#       ③ 首次发布渠道分站加 --expect-p0：与本期预期清单逐条比对（多一条、少一条都失败）
sh scripts/ops/ddl-gate.sh /tmp/preview.sql --expect-p0
```

- 容器里的 `npx -y prisma@5.22.0` 必须带 `npm_config_registry=https://registry.npmmirror.com`：服务器访问 npm 官方源极慢，不带时 2026-09-26 实测卡在下载 7 分钟以上、单次 exec 超时。预览也建议 `setsid nohup` 后台跑、轮询结果文件。
- 期望输出最后一行：`✅ 闸门通过，且与本期预期清单逐条一致（13 张表、31 列、9 个索引、2 条外键）`。
- 清单（设计 5.9 + 主会话 D1）：13 张新表；`orders` 22 列、`order_messages` 3 列、`users` 1 列、`invoices` / `receipts` 各 2 列、`external_orders` 1 列；9 个索引（`orders` 4 个、`invoices` / `receipts` 各 2 个、`external_orders` 1 个）；外键 2 条：`tenant_customers.user_id → users`、`tenant_ledger_entries.order_id → orders`（都是 `ON DELETE RESTRICT`）。
- 危险语句闸门只匹配**语句开头**：新表列名 `payee_changed_at` 含 CHANGE，全文 grep 会每次误报。
- **不是第一次发布**（库里已经有这些表）：预览是 `-- This is an empty migration.`，此时去掉 `--expect-p0` 只过闸门，并确认 A4 的 `SHOW COLUMNS` 阳性对照在。
- 闸门不过：**停手**，别 db push，把 `/tmp/preview.sql` 发回来看。

### A4 先 DDL（旧镜像仍在跑，零影响）

> **元数据锁（MDL）**：ALTER 开始与结束都要短暂拿表的排他 MDL。若有未提交的长事务占着 `orders`，ALTER 会排队，排在它后面的所有读写（下单、到账回调）一起挂住；MySQL 默认 `lock_wait_timeout` 是一年，prisma 又没法设会话级超时。所以：
> 1. 先查长事务：`SELECT trx_id, TIMESTAMPDIFF(SECOND,trx_started,NOW()) FROM information_schema.innodb_trx;`（应为空或都是几秒内）；
> 2. `SET GLOBAL lock_wait_timeout = 30;`（先记下原值）→ db push → 立刻 `SET GLOBAL lock_wait_timeout = <原值>`。拿不到锁时 ALTER 30 秒后失败退出，而不是无限期卡住全站；db push 按差异执行，重跑即可补齐。
> 3. db push 本身也用 `setsid nohup` 后台跑：容器里下载 prisma 要几分钟（2026-09-26 实测 6 分钟，真正执行 DDL 只用了 1.94 秒）。

```bash
docker run --rm --network "$NET" -v "$PWD/prisma":/work/prisma:ro -w /work \
  -e DATABASE_URL="mysql://root:$MYSQL_ROOT_PASSWORD@db:3306/beiguo_shop" \
  -e npm_config_registry=https://registry.npmmirror.com \
  -e PRISMA_ENGINES_MIRROR=https://registry.npmmirror.com/-/binary/prisma \
  node:20-alpine sh -c 'apk add --no-cache openssl >/dev/null && npx -y prisma@5.22.0 db push --skip-generate'
#   ↑ 绝不加 --accept-data-loss；如果它提示会丢数据，说明 schema 不对，停手

docker compose --env-file .env.production exec -T db mysql -uroot -p"$MYSQL_ROOT_PASSWORD" -N -B beiguo_shop -e "
  SHOW TABLES LIKE 'tenants';
  SHOW COLUMNS FROM orders LIKE 'tenant_id';
  SHOW COLUMNS FROM tenants LIKE 'payee_changed_at';
  SELECT COUNT(*), SUM(tenant_id = 1) FROM orders;
  SELECT COUNT(*) FROM orders o LEFT JOIN tenants t ON t.id = o.tenant_id
   WHERE o.tenant_id <> 1 AND (t.id IS NULL OR t.kind <> 'CHANNEL' OR o.created_at < t.created_at);"
```
期望：`tenants`；`tenant_id int NO MUL 1`；`payee_changed_at` 一行；最后一行是 **0**（每次发布都成立：渠道单只能挂在已登记的渠道上、且不早于该渠道建档——存量订单被误标会早于建档而计数）。
倒数第二行的两个计数：**首次发布**时相等（存量订单全部靠列默认值归主站）；**G 之后**渠道站接了单，`tenant_id<>1` 不为 0、两数不等是正常的，不作判据。

**外键带来的锁行为**（D1）：往 `tenant_customers` / `tenant_ledger_entries` 插行时会对被引用的 `users` / `orders` 行加共享锁；代码里凡同一事务既写分录又 UPDATE 订单行，都是先 CAS 订单行、再写分录（锁顺序一致）。删用户 / 删订单前必须先删对应的客户关系 / 分录（`ON DELETE RESTRICT` 会直接拒绝）——生产上本来就不删这两类行。

### A5 种子数据（可重复执行）

```bash
docker exec -i beiguo-db sh -c 'mysql --default-character-set=utf8mb4 -uroot -p"$MYSQL_ROOT_PASSWORD" beiguo_shop' \
  < scripts/sql/tenant-seed.sql | tee /tmp/tenant-seed.out
grep -c FAIL /tmp/tenant-seed.out          # 必须是 0
grep -c 'OK' /tmp/tenant-seed.out          # 期望 6
```
6 条对照在**每次发布**都应全 OK，包括 lulu 开业接单之后（终审第 2 轮：原来的「tenant_id <> 1 的订单为 0」在 G 之后必 FAIL，已改成「渠道单挂在已登记渠道且晚于其建档」这个任何时点都成立的不变式）。输出末尾的 `channel_orders` 只供人工核对：首次发布为 0，G 之后不为 0 属正常。
写入：主站 `tenants(id=1, main, PLATFORM)`；lulu `tenants(id=2, CHANNEL, DRAFT)`，费率 150bp、发票分成 200bp、首月冻结 15 天、最低结算 100 元、申请间隔 7 天；`tenant_domains(lulu.bigolab.com → 2)`。两条 INSERT 都是 `ON DUPLICATE KEY UPDATE` 空操作：重跑不会覆盖站长事后在后台改过的值。**首月过后**由超管在后台把 lulu 冻结期改成 7 天（只影响之后的新订单）。

### A6 构建（服务器上，临时放开 swap；站长已同意，每次执行前在群里说一声）

```bash
# 必须 setsid + </dev/null 完全脱离会话：只用 `nohup … &` 时，workbench exec 会话一结束进程就被连带取消
# （2026-09-26 安全修复部署实录：首次立刻 EXIT=1、无输出）
setsid nohup sh scripts/ops/build-with-swap.sh > /tmp/build-with-swap.out 2>&1 < /dev/null &
# 另起一次 exec 轮询（不在前台等）：
tail -n 30 /tmp/build-with-swap.out          # 最后一行是 RESULT: OK … / RESULT: FAIL …
```

- **日志为空、进程已经不在**：多半是被会话连带杀掉（没用 setsid）或被 `kill -9`——trap 没跑到。先 `ls /tmp/build-with-swap.state`：在就先 `sh scripts/ops/build-with-swap.sh --restore` 还原 swappiness，再重新发起构建。
- **Node 堆上限**：Dockerfile 里 `next build` 的 `--max-old-space-size` 见文件内注释（终审阶段按新代码在开发机实测后定值）；构建日志出现 `JavaScript heap out of memory` 就是它不够——这不是内存组 OOM，swap 与看门狗帮不上，改 Dockerfile 里的值重新构建。

脚本做的事（`scripts/ops/build-with-swap.sh` 文件头有全文说明）：
1. 探测 cgroup 版本：`stat -fc %T /sys/fs/cgroup` 不是 `tmpfs`（v1）就**直接停手**（退出码 3，没改任何设置）。
2. 记录原值后把 `vm.swappiness` 与 buildkit 内存组 `memory.swappiness` 设为 60（buildkit 组在构建开始后才出现，脚本等它出现再写）；原值另存 `/tmp/build-with-swap.state`。
3. 看门狗每 2 秒把 `next build` 进程树 `oom_score_adj` 设为 1000（真 OOM 时先杀构建、不杀站点）。
4. 后台 `docker compose --env-file .env.production build app`，日志写 `/tmp/beiguo-build-*.log`。
5. **无论成败都还原**两项设置（`trap`）。脚本被 `kill -9` 时 trap 跑不到：`sh scripts/ops/build-with-swap.sh --restore` 按状态文件还原（下次运行发现状态文件会拒绝构建，逼你先还原）。
6. 检查 `beiguo-app`、`jishi-app`、`beiguo-db`、`beiguo-nginx`、`beiguo-cloudflared` 与 jishi 的 cloudflared 存活；比对构建前后镜像 ID，**相同即失败**（next build 被 OOM 杀掉会静默 0 退出）。

退出码：0 成功；1 构建失败或镜像没变；2 前置条件不满足（非 root、磁盘不足、残留状态文件）；3 不是 cgroup v1；4 构建后有站点容器不在运行（**立刻人工查看** `dmesg | grep -i oom`）。

构建日志里要看的两行：
- `[tenant-boundary] ✅ 零违规…`（prebuild 边界检查；有违规构建直接失败）。
- `[prerender] routes=N`：根布局动态化之后应为 0（设计 4.8）。集成阶段本地生产构建已核对为 0，Dockerfile 默认 `ARG PRERENDER_STRICT=1`（硬闸）：有页面被预渲染就构建失败，日志列出路由。急着出版本时可临时 `docker compose build --build-arg PRERENDER_STRICT=0`（只打印），事后必须修掉。

### A7 只换 app

```bash
AFTER=$(docker images --no-trunc --format '{{.ID}}' beiguo-shop-app:latest | head -n 1)
docker compose --env-file .env.production up -d --no-deps --force-recreate app
docker inspect -f '{{.Image}}' beiguo-app            # 必须等于 $AFTER
docker compose --env-file .env.production up -d --no-deps --force-recreate cron   # crontab 新增两行（见下）
sleep 60   # cron 容器启动时先换源、apk add curl tzdata，之后才把 crontab 拷到 /etc/crontabs/root；立刻查会得到 0
docker exec beiguo-cron cat /etc/crontabs/root | grep -c 'tenant-'                 # 期望 2（仍是 0 就再等一会儿、看 docker logs beiguo-cron）
```
crontab 新增：每小时第 7 分 `POST /api/cron/tenant-release`（补偿扫描 + 解冻）、每天 03:00 `POST /api/cron/tenant-reconcile`（对账 L1–L13、A1–A13）。都是 `POST + x-cron-secret`。休眠期没有渠道单，只跑几条空查询。

### A8 冒烟（第七节 M1–M14 全部过一遍）+ 私有目录可写

```bash
docker exec -u nextjs beiguo-app sh -c 'touch /app/private/payout/.probe && ls -la /app/private/payout/.probe && rm /app/private/payout/.probe'
docker exec beiguo-app sh -c 'wget -qO- --header "Host: bigolab.com" http://127.0.0.1:3000/partner >/dev/null; echo $?'   # 非 0（404）
```
私有目录（打款凭证）挂的是命名卷 `private_files`，首次挂载继承镜像里 `/app/private` 的 `nextjs` 属主（Dockerfile 预建并 chown，主会话 D11）；不可写时上传凭证会报 500。

### A9 收尾
重新打开营销发送；观察 24 小时（第八节指标）。

---

## 五、N 段：nginx 分流（单独一次发布，不与 A 段混）

前提：真实 IP 修复已上线（`docs/nginx-真实IP修复-部署说明.md`）；A 段已稳定运行。

**N1 统计现网 Host 分布、专查钱路径的 Host**（改 map 之前必须知道有哪些 Host 在用）：
```bash
# nginx:alpine 的 access.log 指向容器 stdout，读 docker 的 json 日志；json 里引号被转义，所以 grep 写 \\"
LOG=$(docker inspect -f '{{.LogPath}}' beiguo-nginx)
tail -n 200000 "$LOG" | grep -o 'host=\\"[^\]*' | sort | uniq -c | sort -rn | head -30                 # 现网 Host 分布
tail -n 200000 "$LOG" | grep -E '/api/(pay/sms-notify|inventory/)' | grep -o 'host=\\"[^\]*' | sort | uniq -c   # 钱路径用的 Host
```
收款回调（SmsForwarder）与 site2 若走服务器 IP 或别的 Host，观察期照常进应用（未知 Host 当主站）；**切严格期前必须把它们登记进 map**。

**N2** 核对 Cloudflare 主站 Public Hostname 是否设了 `httpHostHeader`（设了的话应用收到的 Host 不是访客的 Host，分流会失效）。

**N3** 配置已在仓库（`nginx/nginx.conf`、`nginx/closed.html`、compose 里 nginx 的只读挂载）：
- map `$host → $site_known`：`bigolab.com / www.bigolab.com / localhost / 127.0.0.1 → main`，`lulu.bigolab.com → closed`；
- 观察期：未登记 Host 当主站并记进 `/var/log/nginx/unknown_host.log`（全量 `access.log` 照写）；
- 渠道 Host：`/admin`、`/api/admin` 一律 404；`/api` 只放行白名单前缀（`auth|products|categories|orders|pay/vmq/(create|status)|receipts|invoices|invoice-titles|redeem|partner|account/(profile|unread|overview)`），其余 404；响应加 `X-Robots-Tag: noindex, follow`；
- 主站 Host：`/partner`、`/api/partner` 一律 404；
- `closed`：所有请求 rewrite 到静态停业页（`internal`，直接访问 `/closed.html` 404），不进应用；
- 头清洗：`X-Forwarded-Host` 置空（原来那行 `proxy_set_header X-Forwarded-Host $host;` **替换**为置空——既不再下发 `$host`，客户端自带的也被丢掉；只删掉那行的话 nginx 会把客户端自带的值原样转发）；`x-tenant-id` 置空；
- 8080 口（公网 IP 直连）严格期同样 444（否则 80 口的 444 会被 8080 的 proxy_pass 变成 502）。

```bash
git pull origin main
NET=$(docker inspect -f '{{range $k, $v := .NetworkSettings.Networks}}{{$k}}{{end}}' beiguo-nginx)
docker run --rm --network "$NET" -v "$PWD/nginx/nginx.conf:/etc/nginx/nginx.conf:ro" \
  -v "$PWD/nginx/closed.html:/usr/share/nginx/closed/closed.html:ro" nginx:alpine nginx -t
```

**N4 上线并验证**（单文件 bind-mount，reload 读不到新文件，必须重建容器）：
```bash
docker compose --env-file .env.production up -d --no-deps --force-recreate nginx
docker exec beiguo-nginx nginx -T 2>/dev/null | grep -c 'lulu.bigolab.com'          # 期望 ≥ 1：加载的是新文件
curl -s -o /dev/null -w '%{http_code}\n' -H 'Host: bigolab.com' http://127.0.0.1/                 # 200
curl -s -o /dev/null -w '%{http_code}\n' -H 'Host: bigolab.com' http://127.0.0.1/partner          # 404
curl -s -o /dev/null -w '%{http_code}\n' -H 'Host: bigolab.com' http://127.0.0.1/api/partner/dashboard  # 404
curl -s -H 'Host: lulu.bigolab.com' http://127.0.0.1/ | grep -c '本站暂停访问'                       # 1（停业页）
curl -s -H 'Host: lulu.bigolab.com' http://127.0.0.1/api/products | grep -c '本站暂停访问'           # 1（不进应用）
curl -s -o /dev/null -w '%{http_code}\n' -H 'Host: bigolab.com' http://127.0.0.1/closed.html      # 404（internal）
curl -sI -H 'Host: bigolab.com' http://127.0.0.1/ | grep -ci 'x-robots-tag'                        # 0（主站不带）
curl -s -o /dev/null -H 'Host: zz-unknown.example' http://127.0.0.1/                            # 观察期：未知 Host 照常进应用
docker exec beiguo-nginx tail -n 3 /var/log/nginx/unknown_host.log                               # 上一行的请求记在这里（容器内文件，重建即清空）
docker logs --tail 3 beiguo-nginx 2>/dev/null | grep -c 'host='                                  # 全量访问日志仍在写（≥ 1）
```
再核对：收款回调（下一笔真实小额付款到账）、后台 `/admin` 与 `/api/admin/stats`、安全头与 N4 前一致。渠道 `/api` 白名单在本地已用临时容器验证过（`cross-tenant.ts` T12 nginx 层：245 个 `/api` 路由里白名单放行 66 个、其余 404），线上无需再造 channel 测试 Host。

**N5** Cloudflare Zero Trust → Tunnels → 本站隧道 → Public Hostname → 显式添加 lulu（**不开通配**）：
- Subdomain `lulu`、Domain `bigolab.com`；**Service = `HTTP`，URL = `nginx:80`**（必须进 nginx，不能填 `app:3000`——那样会绕过 nginx 的 Host 分流与渠道 `/api` 白名单）；
- Additional application settings → HTTP Settings → **HTTP Host Header 留空**（设了的话应用收到的 Host 不是 `lulu.bigolab.com`，认不出店面）；
- 保存时 Cloudflare 会自动建 `lulu` 的 DNS CNAME；如果 DNS 里**已有** `lulu` 记录（A / CNAME），先删掉再添加，否则会报冲突或指错地方。

公网打开 `https://lulu.bigolab.com/`、`/products/1`、`/admin`、`/api/products`：**只看到停业页**。

**N6 严格期**（观察一周、`unknown_host.log` 里没有正常流量之后，与应用 `HOST_STRICT=1` **同一天**切）：
1. 把 N1 查到的、仍要用的 Host（例如收款回调用的服务器 IP）登记进 map；
2. `nginx.conf` 里 `map $site_known $site_kind { default $site_known; "" main; }` 的 `"" main;` 改成 `"" "";`；
3. `.env.production` 设 `HOST_STRICT=1`；`nginx -t` → force-recreate nginx 与 app；
4. 验证：随机 Host 与未登记的服务器 IP 得到空响应（444），主站照常。

**N7 回滚命令备好**：`git checkout <上一版> -- nginx/nginx.conf && docker compose --env-file .env.production up -d --no-deps --force-recreate nginx`（只动这一个文件；看完改回 `git checkout HEAD -- nginx/nginx.conf`）。

---

## 六、G 段：lulu 开业（只是配置与数据操作，不构建）

- **G0 前提**：A 段与 N 段都已上线；`TENANT_DATA_KEY` 已配置（录入收款账号要用）；`PLATFORM_HOSTS` 已含站长进后台用的全部 Host（含服务器 IP，如需要）。
- **G1** `.env.production` 设 `CHANNELS_ENABLED=1`，`up -d --no-deps --force-recreate app`（运行时读取，不用重新构建）。此时 lulu 在 nginx 仍是 `closed`，公网看不到任何变化。容器内核对：
  ```bash
  docker exec beiguo-app sh -c 'wget -qS -O /dev/null --header "Host: lulu.bigolab.com" http://127.0.0.1:3000/ 2>&1 | head -1'   # 404（DRAFT，非预览账号）
  docker exec beiguo-app sh -c 'wget -qO- --header "Host: bigolab.com" http://127.0.0.1:3000/api/products | head -c 200'      # 主站照常
  ```
- **G1.5** nginx map 里把 lulu 从 `closed` 改为 `channel`，`nginx -t`，force-recreate nginx；公网 lulu 对非预览用户 404、预览账号可进。
- **G2** 站长在后台「渠道」里：授权商品、按渠道 × 商品设进货价；录入 lulu 的收款账号；邀请渠道主（邀请链接 `/partner/invite/<令牌>`）；渠道主在 DRAFT 期定价上架。
  - **DRAFT 期渠道站不开放注册**（WP1）：渠道主没有账号时，先在**主站**注册（全局同一账号，注册站记为主站），再到 `https://lulu.bigolab.com/partner/login` 登录、点邀请链接接受；之后渠道后台照常使用。站长要在「至少一位渠道主已接受邀请」之后才能把 lulu 改为 ACTIVE（否则 409）。
  - **预览买家**（`previewUserIds`）同理：DRAFT 期前台对未登录者 404、连 `/login` 也打不开，预览买家经 `https://lulu.bigolab.com/partner/login?next=/` 登录后回到店面首页（集成阶段补的入口）。
- **G3 验收**：测试买家账号在 DRAFT 期走全流程（下单 → 付款 → 交付 → 解冻 → 结算 → 打款 → 部分退款 → 冲销）；渠道主走 `/partner` 全部页面；线上只读子集（T2、T3、T5、T6 只 GET；T4 应用层直连只 GET）。**生产不跑写用例、不跑 itest**。数值按设计 10.11 核对：售价 140、进货 110、1.5% → 余额 30.00、打款 27.90；开票（买家付 148.40）→ 余额 32.80、打款 30.66。
- **G4 开业**：后台把 lulu 状态改为 ACTIVE（走后台会写审计；直接 SQL 要补审计：`UPDATE tenants SET status='ACTIVE', updated_at=NOW(3) WHERE id=2`）。立即生效。
- **G5 观察一周**：每日对账、DENIED、VmqLock 占用、内存（第八节）。

---

## 七、冒烟清单（实施分包第 12 节，每次 A 段都跑）

| # | 场景 | 检查点 |
|---|---|---|
| M1 | 登录 / 注册 / 找回密码 / 登出 | 老用户不掉线；新 token 可用 |
| M2 | 普通下单 → 小额真实付款 → 到账 → 自动发卡 → 已支付邮件 | 订单 `tenant_id=1`、快照列为空、无分录 |
| M3 | 内推链接下单 | 返现正确 |
| M4 | 用券下单、抽奖资格、VIP 用户下单 | 与改造前一致 |
| M5 | 勾选开票下单；事后申请开票支付税费；收据 | 金额、发票状态、财务群推送一致；新发票 `tenant_id=1` |
| M6 | 接码商品下单 | 取号、收码、交付 |
| M7 | 后台订单：改价、标已付（实收选填）、标已交付、取消、恢复、补发、留言回复、快捷回复链接 | 行为不变（仅两人同时编辑同一单时后者 409） |
| M8 | 后台卡密分析、用户、发票、收据、外部订单导入与清空；仪表盘 | 「全部」筛选下数字与改造前一致；仪表盘营收与下面 SQL 一致 |
| M9 | 论坛、新闻、游戏、友链、钱包、推荐有奖、营销发送（测试收件人）、营销退订链接、开票填写链接 | 正常 |
| M10 | robots.txt、sitemap.xml、首页与商品页 canonical；交易邮件链接 | 与改造前一致 |
| M11 | 主站 Host 请求 `/partner`、`/api/partner/*` | 404 |
| M12 | `docker stats --no-stream beiguo-app` | RSS 无异常上涨（> 700MB 要评估根布局动态化的影响） |
| M13 | 超管从地址栏 / 新标签页直接打开 GET 导出（订单、卡密、发票） | 照常下载 |
| M14 | 经服务器 IP、localhost、`app` Host 访问 `/admin` 与 `/api/admin/stats` | 照常（`PLATFORM_HOSTS` 默认含后两个；休眠期 middleware 不看 Host） |

M8 仪表盘营收口径：
```sql
SELECT SUM(amount) FROM orders WHERE pay_status = 'PAID' AND delivery_status <> 'CANCELLED';
-- 与改造前的仪表盘数字应当相等（口径未变）
```

运维独立复核（不依赖应用；只读事务）：
```bash
docker exec -i beiguo-db sh -c 'mysql --default-character-set=utf8mb4 -uroot -p"$MYSQL_ROOT_PASSWORD" beiguo_shop' \
  < scripts/sql/tenant-healthcheck.sql
```
每条检查一行，`fail_count = 0` 为通过（L2、L3、L10、L12、L13 钱类；A5、A6、A11 告警类）。钱类失败当天处理，并确认应用对账已把该渠道置 `payoutHold`。

---

## 八、上线后要盯的指标（设计 15.7）

| 指标 | 阈值 | 动作 |
|---|---|---|
| VmqLock 同价槽占用 | > 60% | 下调 `pendingOrderCap` 或暂停渠道 |
| 成员越权 DENIED（审计） | > 0 | 当天查看 |
| 卡密明文查看量 / 渠道订单量 | > 2 | 询问渠道、核对兑换日志 |
| 店面解析查库异常、未知 Host | 异常 > 0；新 Host | 查 DB；查来源 |
| app 内存 | RSS > 700MB | 评估根布局动态渲染的影响 |
| 对账 | 钱类失败 | 应用自动 `payoutHold`（主会话 D7：对账失败默认置 hold）；当日处理 |
| `settle_state='MISSING'` | > 0 | 查原因后按快照补记 |
| 渠道 7 天退款率 | > 10% | 调查 |
| 磁盘 | > 85% | `docker image prune -f`、`docker builder prune -f` |

上线后记一笔：`SELECT @@innodb_rollback_on_timeout;`（设计 17：锁等待超时是否回滚整个事务；代码两种情况都已按「事务可能已回滚」处理）。

---

## 九、回滚与下线

**回滚 app**（只切镜像，**数据库不回滚**）：
```bash
docker tag beiguo-shop-app:rollback-<旧版本号> beiguo-shop-app:latest
docker compose --env-file .env.production up -d --no-deps --force-recreate app
docker inspect -f '{{.Image}}' beiguo-app     # 核对是旧镜像
```
- 旧镜像在新 schema 上照常运行（旧代码不读新列）；新表、新列留在库里。
- **回滚期间与之后任何人不得用旧 commit 的 schema 跑 `db push` 或 `scripts/deploy.sh`**：旧 schema 没有 `tenant_*` 表与新列，`--accept-data-loss` 会删掉整本渠道账。
- **lulu 已开业时，先下线渠道站再动 app**：nginx 里 lulu 改回 `closed` 并 force-recreate nginx，再回滚 app。否则旧代码不认 Host，lulu 会渲染整份主站，这期间的订单记到主站、事后无法区分。
- 不要在部署目录 `git checkout` 旧 commit（会把磁盘上的 schema 换回旧版）；看旧代码用 `git worktree`。
- 回滚到旧镜像后 cron 的两个 `tenant-*` 地址返回 404，只在 cron 日志记一行，不影响业务。
- 前滚：修好后按 A 段重新发布；DDL 已在库里，预览为空是正确的，但要确认 A4 的 `SHOW COLUMNS` 阳性对照在。

**一键下线**（设计 15.6）：

| 层 | 做法 | 生效 | 用于 |
|---|---|---|---|
| 1 应用总开关 | `CHANNELS_ENABLED=0` + force-recreate app | 约 30 秒 | **只能与第 3 层一起用**：先 nginx 改 closed，再关应用开关 |
| 2 租户状态 | 后台把渠道置 `SUSPENDED`（暂停营业横幅、拒绝新单、后台只读）或 `TERMINATED`（停业页；终止仍走结清流程；允许从 SUSPENDED 直接终止，主会话 D7） | 下一次请求 | 对账失败、风控、违约调查 |
| 3 nginx | map 里 lulu 改为 `closed` + force-recreate nginx | 秒级 | 回滚、应用故障、备案整改 |
| 4 Cloudflare | 删除 lulu Public Hostname | 分钟级 | 长期停用 |

渠道功能整体废弃时：下线路由即可；`Tenant` 系列模型与新增列必须保留在 schema 里（加注释「已废弃，勿删」）。

---

## 十、演练记录与本地验证（2026-09-26，开发机，未连服务器）

| 用例 | 做法 | 结果 |
|---|---|---|
| W8-1 边界检查阳性对照 | `--selftest`：在内存里给真实源码树逐类注入 19 处违规（含 partner-services import `ledger`、handlers import `supply-pricing`、login 页不调 `requireChannelStorefrontPage`、新增一处 `invoice.create`） | 19 类全部拦下并指出文件；工作区零违规；营销模块 `domain:` 不误报 |
| W8-3 路由总表 | `merge-routes.mjs --selftest`：新增 partner 路由不登记 / 已有路由加方法不登记 / 新增超管渠道路由不登记 / 登记不存在的路由 | 4 种都失败（符合预期） |
| W8-4 构建脚本 | `build-with-swap.selftest.sh` 在 `nginx:alpine` 临时容器里用假 sysfs 路径跑 | 17 项全过：cgroup v2 → 退出码 3 且不改设置；构建中 kill → 两项还原、子进程不残留；构建进程被杀 → 还原成各自原值；看门狗设 1000；残留状态文件 → 拒绝、`--restore` 还原 |
| W8-5 种子重复执行 | 开发库副本上跑两次（tenants 为空 / 只有 main 两种起点各两次） | 无报错、6 条 OK、行数不变 |
| W8-5b 种子：渠道已接单后重跑（终审第 2 轮补） | 开发库：只有 main 起点跑种子 ×2 → `live-channel.ts --keep`（lulu 开业、两张渠道单已付、已结算打款）→ 再跑种子 ×2 → 阴性对照：把一张 lulu 建档前的存量单改成 `tenant_id=2` 再跑 → 改回再跑 | 只有 main 起点两次：FAIL=0、OK=6；渠道已接单（`channel_orders=2`）两次：FAIL=0、OK=6；阴性对照 FAIL=1（正是「渠道订单都挂在已登记渠道且晚于其建档」那一条）；改回后 FAIL=0、OK=6。A4 的新计数 SQL 在同一现场为 0 |
| W8-6 演练（本地版） | 临时库按基线 `24f5b0f` 的 schema 建表并灌开发库数据 → `node:20-alpine` + `apk add openssl` + `npx -y prisma@5.22.0 migrate diff --from-url`（与 A3 同一条命令，经容器网络连库）→ `ddl-gate.sh --expect-p0` → 同方式 `db push --skip-generate` → 阳性对照 → 种子 ×2 → 巡检 SQL → 再预览 | 预览 15350 字节，闸门逐条一致（13 表、31 列、9 索引、2 外键）；db push 无数据丢失提示；存量订单 `tenant_id` 全为 1；种子两次 FAIL=0；巡检全 0；再预览为 `-- This is an empty migration.`。**生产备份恢复、新镜像 run-all、nextjs 身份 touch 私有目录、回滚旧镜像冒烟**需在服务器临时库 / 发布当天做（本地不能 next build） |
| W8-7 DDL 闸门 | 当前 schema 删 `orders.delivery_info`、改 `products.name` 长度后 `migrate diff` 过闸门；正常增量过 `--expect-p0` | 前者 2 条危险语句被拦下；后者 0 条且清单一致；0 字节预览判失败 |
| W8-8 nginx | `cross-tenant.ts` T12 nginx 层：临时 `nginx:alpine` 跑仓库 `nginx.conf` + 回显上游 | `nginx -t`（观察期、严格期两个副本）通过；lulu closed 只得到停业页；`X-Forwarded-Host` 不再下发（客户端自带的也丢掉）；严格期未知 Host / 服务器 IP → 444 |
| W8-9 文档路径 | `check-tenant-boundary.mjs --doc-paths` | 设计中 99 个 `src/` 路径、改造语境 24 个；差集 5 个，全部注明「只读引用」 |
| 巡检 SQL 阳性对照 | 临时库里把一张已付单改成渠道单、给一张主站单写快照列 | L2、L3a、A11 各报 1 条并给出订单号 |
| 集成：本地生产构建 | `npx next build`（Next 14.2.35，Windows 开发机） | 通过；81 个路由全部 `ƒ`（动态），`.next/prerender-manifest.json` 的 routes=0，所以 Dockerfile 默认 `PRERENDER_STRICT=1`；构建期 node 进程合计峰值约 4.5GB（单进程峰值约 2.2GB，开发机采样，服务器上仍须按 build-with-swap.sh 跑） |
| 集成：休眠态主站回归 | 本地 `next start`（不设 `CHANNELS_ENABLED`）+ 开发库：`itest-security`、`itest-buyer-journey`、`itest-money`（含后台 HTTP 用例）；`Host: lulu.bigolab.com` 直连得到主站页面 | 45/0、11/0、72/0；主站与 lulu Host 的 `/`、robots、sitemap 同为主站，`/partner`、`/api/partner/*` 两边都 404 |
| 集成：渠道主链路（HTTP） | 种子建 lulu（DRAFT）→ `CHANNELS_ENABLED=1` + `TENANT_DATA_KEY` 起服务 → `scripts/itest-tenant/live-channel.ts`（DRAFT 404 → 授权 / 进货价 / 收款账号 / 邀请 → 渠道主主站注册、渠道站登录接受、定价上架 → 开业 → 买家渠道站注册、两单（一张结账开票）、到账、发卡 → 开票 → 解冻 → 申请结算 → 认领 → 登记打款；可见性与跨 Host 令牌） | 76/0。设计 10.11 逐分一致：不开票单余额 3000、打款 2790；开票单（买家付 148.40）余额 3280、打款 3066；结算单 5856。开关打开后主站 `itest-security` 45/0、`itest-buyer-journey` 11/0 |

---

## 十一、部署实录：A 段（2026-09-26 15:24–16:10，休眠发布）

**上线前复核（开发机）**：在集成、两轮终审之后，另做了一次独立复核。
- 机械检查：`tsc` 0 错误；`NODE_OPTIONS=--max-old-space-size=640 npm run build` 通过（routes=0）；`run-all --strict` 16 步全过；休眠态本地 `next start` 跑 itest-security 45/0、buyer-journey 11/0、money 72/0。
- 只读分块审查：5 名审查员按块比对 HEAD 与工作区，每条严重问题由 2 名反驳者核实。确认 1 条 major 并修复：后台订单弹窗「用户备注」改成优先显示 `buyerRemark`，会把 sms.ts / vmq.ts 追加在 `remark` 里的「【待退款】」「待人工补发」提醒藏掉，已改回显示完整的 `remark`。
- 顺带修正：
  - 弹窗明细回来之前管理员已开始编辑时，不再覆盖输入；
  - 兑换前缀限频由 8 放宽到 16；
  - 发布说明第 4 条（营收口径本来就没变）。

**提交**：`161cf96`（415 个文件；`pc.html`、`v2.html` 未提交）。

| 步骤 | 结果 |
|---|---|
| A0 | 服务器 HEAD `22a92d7`、镜像 `a77d47f68697`；磁盘余 11G；宿主机**没有 node**；没有发送中的营销任务（无需关发送） |
| A1 | 回滚标签 `beiguo-shop-app:rollback-22a92d7`（= `a77d47f68697`）；备份 `/opt/beiguo/backups/beiguo_shop_20260926_152402_pretenant.sql`（10.3MB，`Dump completed`） |
| A2 | `git pull` → `161cf96`；临时 node 容器跑边界检查「✅ 零违规」；4 个运维文件在；`grep -c tenant- cron/crontab` = 2；新 `nginx.conf` 用临时容器 `nginx -t` 通过 |
| env | `TENANT_DATA_KEY` 新生成（hex 64 字符，追加前备份 `.env.production.bak-pretenant-*`）。**站长请把它和 JWT_SECRET 一起抄进密码保管处** |
| A3 | 第一次不带 npm 镜像源，`npx` 卡在下载 7 分钟、exec 超时，改为带 `npm_config_registry` 后台跑。预览 15350 字节（与本地演练一致）；闸门「✅ 13 表、31 列、9 索引、2 外键逐条一致，危险语句 0」 |
| A4 | 执行前无进行中事务；`SET GLOBAL lock_wait_timeout=30` → db push（下载 6 分钟，DDL 本身 1.94s）→ 恢复 31536000。阳性对照：`tenants` 在；`orders.tenant_id int NO MUL 1`；2243/2243 单归主站；不变式计数 0。MySQL 8.0.46，`innodb_rollback_on_timeout=0` |
| A5 | 种子 FAIL=0、OK=6；main(1, PLATFORM, ACTIVE)、lulu(2, CHANNEL, DRAFT, 150bp / 200bp / 15 天 / 100 元)、`lulu.bigolab.com → 2` |
| A6 | `build-with-swap.sh` 15:46–16:01；新镜像 `0e1c65f829af`；构建内边界检查零违规、`[prerender] routes=0`；两项 swappiness 还原为 0；6 个站点容器全程存活（nginx 未重启） |
| A7 | 16:01:59 force-recreate app，7 秒就绪；镜像 = `0e1c65f8…`；force-recreate cron，约 45 秒后 `/etc/crontabs/root` 里 `tenant-` = 2 |
| A8 | 私有目录以 nextjs 身份可写。容器内：`/`、`/products`、`/api/products`、`/login`、`robots.txt`、`sitemap.xml` 200，`/partner`、`/api/partner/dashboard` 404，`/admin` 307，`/api/admin/stats` 401。公网：同样结果，商品页 title、canonical、Product / Organization / BreadcrumbList 结构化数据都在，sitemap 442 条，安全头在。容器 env：`CHANNELS_ENABLED` 空（休眠），`TENANT_DATA_KEY` 64 字符。巡检 SQL 12 项 fail_count 全 0。app 内存 81MB。启动后无错误日志 |

**N1 预查（只读）**：nginx 日志约 10 小时，Host 分布为 bigolab.com 8028、服务器 IP 182.92.209.103 573（扫描器与爬虫，另有 4 次 `POST /api/admin/login` 探测）、www 45、`_` 7、其他 2。收款回调与库存接口 18 次全部走 bigolab.com。观察期「未登记 Host 当主站」不会误伤现网流量；严格期之前不需要把服务器 IP 登记为收款 Host。

**A 段真实付款验证**：新镜像上第一笔真实付款，16:15 下单、750.00 元，45 秒后自动到账并发货。订单 `tenant_id=1`、`settle_state` 为空、`buyer_remark` 已双写；渠道分录表 0 行。这段时间的 `prisma:error` 全部是 `pageView.create` 唯一键冲突（原有的浏览去重，预期内）。

## 十二、部署实录：N 段（2026-09-26 16:18）

- N1：见上（钱路径全走 bigolab.com）。N2：cloudflared 日志显示主站入口是 `ingressRule=0 originService=http://nginx:80`。N5 之前查了 DNS：lulu 没有任何记录（NXDOMAIN），不用先删。
- N3：`nginx -t` 通过。N4：16:18:40 force-recreate nginx。加载的是新文件（`nginx -T` 含 lulu）。
- 8080 口（宿主机 80 映射）与容器 80 口（隧道入口）两条路径结果一致：
  - 主站 `/`、`/products`、`/api/products`、`www` 返回 200；
  - `/partner`、`/api/partner/dashboard`、`/closed.html` 返回 404；
  - lulu 的 `/`、`/api/products`、`/admin` 全部只得到停业页（「本站暂停访问」）；
  - 未知 Host 照常进应用，并记进 `unknown_host.log`；
  - 主站不带 `X-Robots-Tag`；全量访问日志照常写。
- 公网：主站 200、`/partner` 404，访问日志 `cf_ip` 仍是访客真实 IP。
- 回滚命令（N7）：`git checkout 22a92d7 -- nginx/nginx.conf && docker compose --env-file .env.production up -d --no-deps --force-recreate nginx`。

**待办**：G 段。站长先按 N5 在 Cloudflare 加 lulu 路由（加完后公网只看到停业页），再定开业时间。

---

## 附：本期新增 / 修改的运维文件

| 文件 | 用途 |
|---|---|
| `scripts/check-tenant-boundary.mjs` | 构建期边界检查（prebuild）；`--selftest` / `--strict` / `--doc-paths` / `--root` |
| `scripts/itest-tenant/merge-routes.mjs`、`scripts/itest-tenant.routes.json` | 路由总表合并与核对（生成物入库） |
| `scripts/itest-tenant/run-all.ts`、`cross-tenant.ts` | 渠道测试总入口、跨租户总扫描 |
| `scripts/itest-tenant/live-channel.ts` | 本地生产构建的渠道主链路 HTTP 冒烟（要先起服务；`--cleanup` 只清理、`--keep` 保留现场） |
| `scripts/ops/build-with-swap.sh`（+ `.selftest.sh`） | 服务器带 swap 构建 |
| `scripts/ops/ddl-gate.sh` | DDL 预览闸门 + 本期预期清单 |
| `scripts/sql/tenant-seed.sql`、`tenant-healthcheck.sql` | 种子、只读巡检 |
| `nginx/nginx.conf`、`nginx/closed.html` | Host 分流、停业页 |
| `Dockerfile` | prerender 清单打印（可切硬闸）、预建 `/app/private/payout` |
| `docker-compose.yml` | app 环境变量（裸变量名）、`private_files` 卷、nginx 挂停业页 |
| `.env.production.example` | 渠道分站变量说明 |
| `package.json` | `prebuild`、`itest:tenant` |
| `cron/crontab` | 新增 `tenant-release`（每小时）、`tenant-reconcile`（每日 03:00）两行——已跟踪文件的修改，提交时别漏（第二节提交清单） |
