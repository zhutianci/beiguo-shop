# 贝果科技 · 项目交接与待办

> 最后更新：**2026-09-07（第二轮，A 组 + B1）**
> 上一轮工作区间：2026-09-04 ~ 09-07，17 个提交（`67863c2` → `f0cefe2`）
> 本轮：A1 去窗口 / A2 全文层 / A3 样式迁移 / B1 AIHOT 线索接入，见第十节
> 线上运营中（订单 1771+）

新会话请先读这一份，再读 `.claude/skills/ai-news-pipeline/SKILL.md`（AI圈大事记的契约）。

---

## 一、项目是什么

**bigolab.com** — AI 订阅代开销售站（Claude Pro/MAX、ChatGPT Plus/Pro 等），真实在营业、有真实买家和收入。

| | |
|---|---|
| 代码 | `D:\selfData\code\selling system\ai-service-shop` |
| 技术栈 | Next.js 14 App Router + Prisma + MySQL 8 + Docker |
| 服务器 | 阿里云 ECS `i-2ze8s6my1msl5q6thpum`，**region 是 cn-beijing**（不是杭州，默认 region 查不到） |
| 部署路径 | `/opt/beiguo/beiguo-shop` |
| 域名 | **未备案**，走 Cloudflare Tunnel 出站规避拦截 |
| 机器规格 | 1.8G 内存 + 2G swap，磁盘 40G（用了约 25G） |
| GitHub | `zhutianci/beiguo-shop`，分支 `main`（服务器 remote 走 `ghfast.top` 代理） |

**这是生产环境，不是玩具。** 任何改动前先备份，见第五节。

---

## 二、怎么操作服务器（重要，能省一小时）

用阿里云 Workbench CLI，本机路径 `C:\Users\zbb\AppData\Local\Programs\workbench\workbench.exe`（已加入用户 PATH），凭证在 `~/.workbench/config.json`。

```powershell
& "$env:LOCALAPPDATA\Programs\workbench\workbench.exe" exec -i i-2ze8s6my1msl5q6thpum -r cn-beijing -c "<命令>"
```

### 三个必须知道的坑

**① PowerShell 会把命令拆坏。** 带空格、引号、或前导 `--` 的参数会被 PowerShell 重新分词，远程命令直接变形（实际踩过：`git log --format="%H %s"` 被拆成三段）。**所有远程命令一律 base64 传输**：

```powershell
$script = @'
<多行 bash>
'@
$norm = $script -replace "`r`n","`n"
$b64=[Convert]::ToBase64String([Text.Encoding]::UTF8.GetBytes($norm))
& $wb exec -i i-2ze8s6my1msl5q6thpum -r cn-beijing --timeout 600 -c "echo $b64 | base64 -d | bash"
```

**② exec 会话对长输出会中途截断。** 耗时命令（build / db push / 管线）必须后台跑再轮询：

```bash
nohup <命令> > /tmp/x.log 2>&1 &
# 之后另起一次 exec：
for i in $(seq 1 280); do pgrep -f "<关键词>" >/dev/null || break; sleep 3; done
cat /tmp/x.log
```

**③ 本地 sleep 被工具链拦截**，等待逻辑要写在服务器端的 bash 里。

### 常用命令

```bash
# 部署（改代码后）
cd /opt/beiguo/beiguo-shop && git pull origin main
docker compose --env-file .env.production build app     # 约 6-7 分钟
docker compose --env-file .env.production up -d

# 改了 cron/crontab 必须重建 cron 容器才生效
docker compose --env-file .env.production up -d --force-recreate cron

# 查 MySQL（必须带 --default-character-set=utf8mb4，否则中文全是 ?）
docker compose --env-file .env.production exec -T db \
  mysql -uroot -p"$MYSQL_ROOT_PASSWORD" --default-character-set=utf8mb4 -N -B beiguo_shop -e "SQL"

# 手动触发新闻管线（务必从 cron 容器直连 app:3000，不要走 nginx——
# nginx proxy_read_timeout 只有 90s，管线跑不完会 504，但服务端仍在跑并占着锁）
docker compose --env-file .env.production exec -T cron sh -c \
  "curl -s -m 1500 'http://app:3000/api/cron/news?stage=collect,triage,cluster,compose,rank&secret=$CRON_SECRET'"
```

**git push 在 Bash 工具里会 SSL_ERROR_SYSCALL，必须用 PowerShell 工具 push。**

---

## 三、这一轮做了什么（17 个提交）

### 大功能

| 功能 | 说明 |
|---|---|
| **全站列表分页** | 20 个列表页原本只有 3 个有分页。发票管理原本是「全表读进内存再过滤排序」 |
| **卡密对象化** | 成本/售价/利润落库不现算；创建时间与发出时间拆两列；批量改成本/售价；已售卡可点开看订单 |
| **卡密数据分析** | 仪表盘新增每日成本/流水/利润与商品分布 |
| **订单管理** | 日期区间 + 商品分类筛选 + 每单成本利润 |
| **收款监控** | VmqApk 全线移除，只留 SmsForwarder；金额识别改强信号匹配 |
| **收据** | 来源标记 + 手动 DIY 开具（条目可拖动排序、金额自动转大写） |
| **用户详情页** | `/admin/users/[id]`，订单/付款/余额流水各自分页 |
| **发票必选项** | 「是否展示 ChatGPT/Claude 字眼」 |
| **站点公告** | 后台发布，买家进前台弹窗（三态：普通/强提醒/定时生效） |
| **AI圈大事记** | 见第四节，单独讲 |
| **企业微信推送** | 下单/支付/开票/收据/留言/库存告警/注册，含一键回复与财务开票台 |
| **桌面端适配** | 导航字号阶梯、行长控制、弹窗宽度分档 |

### 修掉的现存缺陷（不少是踩雷才发现的）

1. **`POST /api/receipts` 与 `/api/invoices` 零鉴权** — `externalOrderId` 是自增整数，任何人遍历就能给别人订单开票据、拿到 token 看到对方邮箱与金额。已加 `assertExternalOrderAccess`（登录本人/已绑定账户/匿名需提供正确账户邮箱）
2. **卡密解密密钥每次重算** — `scryptSync` 约 22ms×200 行 = 单次请求 4.5 秒同步阻塞，期间整站无响应。已加进程内缓存
3. **后台手工置「已完成」销量双计** — 付款履约已 `sales++` 过一次
4. **金额大写长期算错** — 1430 印成「壹仟肆佰叁拾**零**元整」、10001 漏零。收据是盖公章的凭证，已重写并补 15 条断言
5. **买家订单接口 `{...o}` 整行外泄** — 加成本/利润列会把毛利发给买家，改显式 select
6. **`.env.production.example` 里 AI圈大事记 配置块重复** — 七个键各出现两次，`.env` 后者覆盖前者
7. **`ORDER_MSG_WEBHOOK_URL` 疑似哑火** — 后经查证是**误判**，见第六节

---

## 四、AI圈大事记（最复杂的模块）

**先读 `.claude/skills/ai-news-pipeline/SKILL.md`**，那是契约，改动前必看。

### 定位（这条决定其他所有设计）

**是「行业动态聚合工具」，不是新闻站。** 《互联网新闻信息服务管理规定》要求的许可拿不到，策略是让内容不落入其定义：只做自写摘要 + 外链原文，绝不转载；不碰政策/监管/地缘/裁员等「社会公共事务」选题；作者硬编码「贝果科技 AI 资讯助手」，不署真人名；AI 生成标识必须出现在 6 处（法定义务）。

**为内容质量拆掉这条基础，不划算。** 上一轮用户两次要求「搬运 aihot.virxact.com 的内容」，都按此原则挡下并给了替代方案 —— 见待办。

### 管线（六段，各自独立抢锁，任一段失败不影响其他段）

```
collect  每小时05分  抓 feed（零 LLM、零依赖、单源熔断）
triage   每小时05分  分诊：是否 AI 相关、是否命中黑名单、归类
cluster  每小时15分  同事件聚合（urlHash 免费快路 + 实体指纹 + 小模型判定，不用向量）
compose  每小时25分  抓原文正文 → 写摘要/推荐理由/AI评分（唯一的写作题）
rank     每15分钟    重算热度（明细落 scoreDebug，可解释）
digest   21:00/周一09:00  日报周报
```

### LLM

当前用**智谱 GLM**（`glm-4-flash` 分诊 / `glm-4-plus` 写作）。境内直连实测：GLM 401/55ms、DeepSeek 401/87ms、百炼 401/34ms 全部可达；**`api.openai.com` 超时、`api.anthropic.com` 403，均不可用**。换供应商只改 `LLM_PROVIDER` + `LLM_API_KEY` + model 名。

日预算硬闸 `NEWS_DAILY_BUDGET_CENTS=300`（3 元/天），超支当天降级为「只去重不写摘要」。

### 信源可达性（2026-09-04 从该 ECS 实测，新增源前必须实测）

✅ 机器之心、量子位、36氪、InfoQ、少数派、掘金、AIbase、通义博客、IT之家、**hnrss.org**、TechCrunch:AI、The Verge:AI、Ars Technica:AI、MIT TechReview、The Decoder、微软研究院、OpenAI RSS、Anthropic News、GitHub API、arXiv API、Simon Willison

❌ **X/Twitter、Reddit、HuggingFace（含 hf-mirror）、DeepMind、Meta AI、Mistral、Google News、V2EX、RSSHub 公共实例、CSDN、知乎热榜**

⛔ **开源中国已停用** — 它的 news feed 是码云项目发布流水（「灵界 OS v4.0.0」），题材不符且单次 40 条会刷屏

**媒体源用「AI 专版」RSS 而不是全站版** — 全站版里手机汽车游戏占大头，每条都要花一次分诊调用才能剔除，等于为噪音付费。

### 踩过的坑（都是线上真实发生的）

1. **glm-4-flash 吐裸对象** — 提示词里的 JSON 示例被 `join('\n')` 拆成两行，模型认成内层对象形状，把最外层 `results` 数组整个丢掉。**给模型的 JSON 示例必须完整在同一行**
2. **批量越大越浪费** — 给 8 条只答第一条。`NEWS_TRIAGE_BATCH=3` 后每批答全，延迟反而更低
3. **漏答条目被永久判死** — 原逻辑标 `FAILED` 永不重试，40 条丢了 35 条。已改为留 RAW 重试，7 天陈旧清理兜底
4. **素材被锁死在一句话** — 只用 feed 的 description，摘要写不出细节，换强模型也没用。已加 `lib/news/extract.ts` 抓原文正文（零依赖启发式提取），效果显著：

   > 改前：阿里已发表两篇关于循环Transformer的顶会论文，该技术因GPT-6而受到关注。
   > 改后：…提出 MeSH 和 SpiralFormer 解决方案…MeSH 使模型减少 33% 非嵌入参数的同时，准确率提升 1.06 个百分点，额外计算开销仅 0.014%。

---

## 五、备份与回退

```
数据库备份  /opt/beiguo/backups/beiguo_shop_20260906_150025.sql.gz   （29 表，已校验 Dump completed）
            /opt/beiguo/backups/beiguo_shop_20260904_162250.sql.gz
镜像标签    beiguo-shop-app:rollback-c2a2dda   （2026-09-06 大更新前）
            beiguo-shop-app:rollback-67863c2   （2026-09-04 大更新前）
```

### 改 schema 前的标准动作

```bash
# ① 备份
docker compose exec -T db mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" \
  --single-transaction --routines --triggers --set-gtid-purged=OFF beiguo_shop > /opt/beiguo/backups/x.sql
tail -3 x.sql | grep "Dump completed"   # 必须校验，否则可能是个半截文件

# ② 预览 DDL，确认零 DROP（scripts/deploy.sh 写死了 --accept-data-loss，不会有任何提示）
docker run --rm --network beiguo-shop_app-network -v "$PWD/prisma":/work/prisma -w /work \
  -e DATABASE_URL="mysql://root:$MYSQL_ROOT_PASSWORD@db:3306/beiguo_shop" \
  -e PRISMA_ENGINES_MIRROR=https://registry.npmmirror.com/-/binary/prisma \
  node:20-alpine sh -c 'apk add --no-cache openssl; npx -y prisma@5.22.0 migrate diff \
    --from-schema-datasource prisma/schema.prisma --to-schema-datamodel prisma/schema.prisma --script'

# ③ 确认安全后再 db push（把上面最后一行换成 db push --skip-generate）
```

**回退**：`git checkout <commit>` → `docker tag beiguo-shop-app:rollback-xxx beiguo-shop-app:latest` → `up -d`。
新增的列都是可空的，旧代码不读，**回滚代码不需要回滚数据库**。

---

## 六、血泪教训（同一个坑不要踩第二次）

### 1. 我自己造成过两次线上故障

**crontab 被删光字母 r。** 容器命令里写了 `tr -d '\r'` 想防 CRLF，反斜杠被 YAML 折叠 + shell 双引号两层处理吃掉，变成 `tr -d 'r'` —— `curl`→`cul`、`cron`→`con`，**全部定时任务静默失效且 crond 不报错**，连原有的收款单清理和接码轮询一起挂。现已改用 `.gitattributes` 强制 LF，容器命令里只用 `cp`。**不要再往那条命令里加任何带反斜杠的转义。**

**改 TZ 引发锁永不自愈。** 把 app 容器从 UTC 改成 `Asia/Shanghai` 后，MySQL 的 `DATETIME` 不带时区，改动前写入的行被按新时区重新解释后落到「未来」，`now - createdAt` 成负数恒小于 TTL。表现：MySQL 算 27 分钟，Node 认为没到 15 分钟。已改用绝对值判断。**凡是比较 MySQL DATETIME 与当前时间的地方都要留意这个。**

### 2. 排查前先验证，别急着下结论

我曾断言「`ORDER_MSG_WEBHOOK_URL` 从没传进容器，留言推送一直是哑的」，依据是 `printenv` 里没有。**错了** —— `docker compose exec` 起的是另一个进程，不走 Next 的启动流程。真实链路是 Next standalone 把 `.env.production` 打进镜像，`server.js` 启动时用 `loadEnvConfig` 读进 `process.env`。用户的留言通知一直是正常的。

**由此引出一条 compose 写法铁律**：新增环境变量一律用**裸变量名**（`- FOO`），不要写 `- FOO=${FOO}`。后者在 FOO 未定义时会给容器一个**空字符串**，而 dotenv 遵循「已存在的 env 优先」，反而把镜像内 `.env` 的真实值**遮蔽掉** —— 那会让本来正常的功能失效。

### 3. 我又造成过一次全站宕机：`docker system df` 把 dockerd 撑爆了

2026-09-07 14:13，为了评估构建空间跑了 `docker system df` / `docker builder du` /
`du -sh /var/lib/docker/overlay2`。这几条要遍历所有镜像层算尺寸，
而这台机器有 **36 个悬空镜像**、overlay2 目录硬链接展开约 130G。
`dockerd` 的 RSS 冲到 1.4G（全机 1.87G），被内核 OOM 杀掉，**连杀 5 次**
（14:13:50 / 14:17:45 / 14:19:06 / 14:25:01 / 14:30:17）。
dockerd 被 SIGKILL 时它名下的容器随之消失，**beiguo 与 jishi 两个站点同时下线约 20 分钟**。
`restart: unless-stopped` 救不了这种情况 —— 容器不是「退出」，是随父进程一起没了。

**这台机器上永远不要跑**：`docker system df`、`docker builder du`、`du /var/lib/docker`。
看空间用 `df -h`（文件系统层面）+ `docker images`（只读元数据、不算尺寸）。
真要清理直接 `docker image prune -f`，不要先去「统计一下有多少」。

顺带：`du -sh /var/lib/docker/overlay2` 报出来的 130G 是**假的**（overlay2 用硬链接，
du 会重复计数），磁盘实际只有 40G。想知道真实占用只能看 `df -h`。

### 4. 其他

- **alpine 镜像不装 tzdata，`TZ` 会被静默忽略**，进程仍跑 UTC，不报错
- **`.next` 属主是 root 而运行时是 nextjs 用户** — 不 chown 会每次请求报 EACCES 并退化成完全不缓存
- **`route.ts` 只能导出 HTTP handler 和少数配置项**，多导出一个 schema 会让 `next build` 报「不是合法的 Route export」，公用校验放 `lib/`
- **动态路由 params 签名混用** — 26 个里只有 6 个是 Next15 的 `Promise<{id}>`，新文件跟同目录已有写法保持一致，混用会 build 失败
- **`docker compose exec` 的 stderr 会被 compose 的 WARN 淹没**，查东西时用 `2>/dev/null` 而不是 `2>&1`
- **mysql 客户端不带 `--default-character-set=utf8mb4` 中文全变 `?`**
- **中文限宽用 px 不用 ch** — `ch` 按西文 `0` 宽度算，对中文会算出接近实际字数一半的宽度

---

## 七、待办

### A. 已完成（2026-09-07 第二轮）—— 详见第十节

原文保留在下面，便于对照做了什么、以及**刻意没做什么**。

**A1. 去掉大事记的 7 天窗口**
`compose` 只给 7 天内的事件写摘要、`rank` 只重算 7 天内热度，超期的永远处理不了 —— 表现为「只有最近一批」，与用户要的「增量发布、旧资讯不消失」冲突。
改法：去掉窗口，改成按热度+新鲜度排优先级逐轮消化积压；再加**按月归档**（`/news/2026-09`）让累积在页面上看得见。

**A2. 一条资讯拆成简要 + 全文两层**
现在只有一段摘要，信息量撑不起详情页。已经能抓到原文正文了，应该产出两层：
- `summary` 120–260 字（列表卡片用，已有）
- `detail` 600–1200 字、分 2–4 段带小标题（详情页用，**待新增**）
`detail` 仍是自写的，不是原文。需要加 schema 字段 + 一次写作调用。

**A3. 页面级 `pt-32` 迁移到 `.page-top`**
桌面端头部已从 113px 收到 97px，`globals.css` 里备好了 `--header-h` 与 `.page-top`，但约 20 个页面仍硬编码 `pt-32`。不换不会坏，只是桌面端比设计值多空 16px。同时 `news-stream.tsx:400` 的 `scroll-mt-28` 和 `support/page.tsx` 的 `scroll-mt-24` 也是硬编码，应换成 `.scroll-below-header`。

### B. 等用户决定

**B1. 推特信源 —— 授权已拿到，已接入（2026-09-07）**

授权号 `AIHOTAPI20260907001`。实现与授权边界的书面对照表见 `docs/AIHOT线索接入.md`。
信源出厂 `enabled: false`，**开启前先确认 `/admin/news` 的「待写摘要（积压）」已稳定在低位**。
下面是当时的调研记录，保留备查：
用户两次要求「搬运 aihot.virxact.com」。查证结果：
- 该站**有完整公开 API**（`/api/v1/items`、`/api/v1/hot-topics`、MCP、多种 RSS），robots.txt 明确 `Allow: /api/v1/`，匿名只读无需 key
- **但其《公开使用规则》§4 明确要求**：面向客户/公众的商业产品、**导流**、批量公开再分发，**须先取得书面授权**；并明确「接口公开、无需 API Key、只展示摘要，都不能代替授权」
- bigolab.com 卖订阅、大事记用于导流，落在「外部商业使用」，无模糊空间

**已给用户写好授权申请邮件草稿**（收件人 `wzglyay@virxact.com`），申请范围刻意克制：只取「选题发现信号」（标题/原文链接/来源名/发布时间），摘要仍由我们抓原文自写，页面标注「线索来源：AIHOT」并回链。**拿到书面回复即可接入，工作量约 1 小时**（API 已摸清）。

X 直连不可行的技术原因：未登录看不到时间线、数据中心 IP 全段封禁（ECS 和 Cloudflare Worker 出口都在内）、nitter 类镜像已全部关停。替代方案：官方 API Basic 约 $200/月，或继续吃 TechCrunch/The Decoder 的二手覆盖。

**B2. 深色/浅色/跟随系统三态主题**
用户提过但当前**只做了 PC 适配、三态主题往后放**。紧急的白底白字已通过声明 `color-scheme: dark` 修掉。真做三态要把 22 个前台页面硬编码的 `text-white/xx`、`bg-white/xx` 全部抽成 CSS 变量，是个独立项目。

**B3. 企业微信群内直接回复**
群机器人 webhook 是**单向的**（腾讯设计限制，只能发不能收）。当前方案是通知里带免登录的一键回复链接（`/reply/[token]`，HMAC 签名、限定单张订单、7 天过期）。
要在群里直接打字回复，需要用户在企微后台建**智能机器人或自建应用**并配消息回调，服务端还要实现腾讯的 AES-256-CBC 解密与签名校验。依赖用户的企业管理员权限。

### C. 建议做但没提过

- **财务开票台目前免登录**（`/finance/[token]`，3 天过期）。谁拿到链接就能看到所有待开发票的抬头与税号。若财务是固定的人，给他开后台账号更稳妥
- **`/admin/news` 待复核队列**要定期看。当前所有事件几乎都被标 `needsReview`，说明阈值可能过严，跑几天后按实际情况调
- **信源质量要看几天数据才准**。这一轮已证明「1 个好源胜过 3 个凑数的」
- **X 相关的 9 个源已配好**（`x-openai`、`x-sama`、`x-gdb`、`x-thomwolf` 等），`enabled: false`，接上 X API 或中继后改开关即可
- **`NEWS_RELAY_URL` 未配置** — Cloudflare Worker 代码在 `cloudflare/news-relay.worker.js`，部署文档在 `docs/新闻中继与X信源.md`。配了能救回 DeepMind/Meta/Mistral/HuggingFace/Reddit（但救不回 X）。**Worker 里的域名白名单不要删**，删了等于开了公开 SSRF 代理

---

## 八、当前线上待用户确认的事

1. **暗夜模式修复** — 请用手机开着暗夜模式访问订单页，确认卡密可见（这是最直接影响买家收货的一处）
2. **PC 端效果** — 导航栏字号间距、1920 宽屏首页是否太空、768~820px 订单页筛选条是否换行
3. **企业微信通知** — 后台「系统设置 → 发送测试消息」验证联通
4. **发票流程** — 税费到账后会推送完整信息 + 待开清单 + 开票台链接
5. **智谱 key** 已配置生效（`glm-4-flash` / `glm-4-plus`）。**该 key 曾在对话中明文出现过，建议重新生成一个**。同理，阿里云 AccessKey 也曾明文出现，建议禁用重建并换成只授权 ECS 会话管理的 RAM 子用户

---

## 九、相关文档

| 文档 | 内容 |
|---|---|
| `.claude/skills/ai-news-pipeline/SKILL.md` | 大事记契约：法律定性、选题黑名单、信源分级、五段管线、LLM 抽象、AI 标识六处 |
| `docs/本次升级部署说明.md` | 2026-09-04 那一轮的 schema 变更与部署命令 |
| `docs/新闻中继与X信源.md` | Cloudflare Worker 中继部署 + X 信源现状 |
| `docs/共用库存发卡API.md` | 第二站共享卡密库存的契约 |
| `VMQ_SETUP.md` | 收款监控（已改为纯 SmsForwarder） |
| `scripts/check-money.ts` | 金额逻辑自测（分摊、大写、浮点），不需要数据库 |
| `scripts/check-amount-parse.ts` | 到账文案识别自测 |
| `prisma/backfill-cardkey-cost.ts` | 卡密成本回填（可重入、dry-run） |

---

## 十、本轮（2026-09-07 第二轮）做了什么

### A1 · 去掉 7 天窗口，改增量累积 + 按月归档

原来有四处「7 天」，**只该去掉两处**：

| 位置 | 处置 | 理由 |
|---|---|---|
| `compose()` 的 `since` | **去掉** | 就是它让超期事件永远轮不到，表现为「只有最近一批」 |
| `rank()` 的 `RANK_WINDOW_DAYS` | **去掉**，改分层 | 同上 |
| `cluster()` 的 pending 窗口 | 放宽到 14 天，**并加终态** | 原来超期条目会永远停在 `eventId=null`：既不成事件也不被清理（purge 只删孤儿，而它们的 `triageState` 是 OK）。现在超龄判 `SKIP` |
| `collect()` 的 `MAX_ITEM_AGE_DAYS` | **保持 7 天不动** | 这是**收集窗口**不是处理窗口，它咬合着 30 天保留期与 `TRIAGE_STALE_DAYS`。放宽只会「花钱分诊 → 被保留期删掉 → 下轮又插回来」 |

**compose 改双车道**：新鲜（3 天内，3 条/轮）+ 积压（更老，2 条/轮）。
名额**固定切分**，不是「优先新鲜、有剩再给积压」—— 后者在信源稳定产出时永远剩不下名额。
积压排序按 `sourceCount / tier1Count` 而不是 `score`：score 带 36h 半衰期，
跨越几十天的候选之间衰减差几个数量级，按 score 排等价于纯按时间排。

**compose 加墙钟 7 分钟**。crontab 的 `--max-time 540` 是硬上限，到点 curl 断开、
服务端还在跑并占着锁。**锁 TTL 永远不能大于 540s**，只能让 compose 自己提前收工。

**rank 改分层**：热层（72h）每轮、温层（72h–14d）每小时一次、冷层只补 `baseScore` 为空的。
新增 `base_score` 列（不含衰减的原始分）：`score` 是 `Decimal(8,3)`，约三周后老事件全 round 成
`0.000`，归档页就排不出先后。历史行为 NULL，由冷层每轮补 200 条自愈，**不写一次性回填脚本**。

**归档页 `/news/archive/2026-09`**（不是 `/news/2026-09`）。后者要在 `[slug]` 这个动态段里
靠正则分流「2026-09」和「2026-09-06-a1b2c3d4e5」—— 正则少写一个锚点，
**全部新闻详情页会当场变成归档页**。为省 8 个字符引入一个全站级故障点不划算。
`/news` 底部有「按月回看」入口，sitemap 收录最近 24 个月的归档页。

**「最近补录」折叠行**：积压车道写出来的老事件按 `happenedAt` 排会沉到时间流中间，
首屏看不见。这一行是它们唯一的露出位置，**复用 `HIGHLIGHT_WHERE`**（含 `needsReview:false`）——
「happenedAt 很旧 + publishedAt 很新」几乎就是降级发布条目的特征，不过滤会精准地把最不该推的推上去。

### A2 · 全文层 detail（600–1200 字，2–4 段带小标题）

新增三列：`detail`(Text)、`detail_state`(RAW/DONE/SKIP)、`detail_tries`(Int)。

- **新事件**：跟摘要一次生成（compose 仍是「唯一的写作题」），`maxTokens` 900 → 2200
- **老事件**：独立的回填通道，**默认关闭**（`NEWS_DETAIL_BACKFILL=1` 开启）。
  三道闸：每轮最多 2 条、当日花费超预算 40% 就停、单条最多重试 3 次
- **detail 是可丢弃区块**：段数/字数/连续 18 字重合/数字回查/禁词/选题黑名单，
  任何一项不过就只丢这一块，事件照常发布。**不进 `needsReview` 队列** ——
  页面退回「只有摘要」本身就是合规状态，没有需要人看的残留
- **瞬时失败绝不写终态**：超时/限流/5xx 只 `detail_tries+1` 下轮重来。
  第四节踩坑 3（40 条丢 35 条）就是把瞬时失败当内容性结论写死导致的
- 后台可按段人工编辑，**刻意不提供「重新生成」按钮**——那会是无次数上限的付费入口

**顺带修了 `lib/llm.ts` 的记账**（这是 A2 的前置，不修就没有可信的成本数据）：
① 三级降级的 token 改累加（原来后一次覆盖前一次）
② 失败调用按真实 token 计费（原来写死 0，导致失败率一升高账面花费反而不动）
③ 日界改固定 +8 算术（原来用进程本地时区，alpine 无 tzdata 时会偏 8 小时）

### A3 · pt-32 → .page-top

20 个文件 + `globals.css`。全部在 `(shop)` 布局的 `.shop-shell` 子树内，无一例外。
`/news` 两页原是 `pt-28 sm:pt-32`（手机刻意贴头部），改成 `pt-28 sm:page-top`。

**实测过一件事**：Tailwind v3 **会**为 `@layer components` 里的自定义类生成 `sm:` 变体，
产物里 `.sm\:page-top`（1088 行）排在 `.pt-28`（689 行）之后，覆盖可靠。
我一开始为此另造了一个 `.page-top-compact` 类，属于多余，已删掉。

### B1 · AIHOT 线索接入

见 `docs/AIHOT线索接入.md`（授权边界的书面对照表）。要点：

- 只取「去哪儿看」：标题 / 原文链接 / 原发布者名 / 发布时间。
  对方的 `summary` / `reason` / `score` / `category` **一个都不取**
- 边界靠**类型系统**守：zod 白名单 shape 剥掉未声明的键，`AihotLead` 类型里根本没有
  `summary` 这个字段名，想往下游传过不了 tsc。`scripts/check-news-a1b1.ts` 有对应断言
- **线索条目的 `summary_raw` 永远是 NULL，这是设计不是 bug**，不要「顺手补上」
- triage 段为线索**先抓原文正文**再分诊（只拿标题分诊会让黑名单判定明显变松）；
  抓不到直接判 SKIP —— 没素材就写不出自写摘要
- 线索**不计入 `sourceCount`**：计进去会让「单源 tier3 → 待复核」闸门对线索条目永不触发
- 信源出厂 `enabled: false`

**91 条样本的实测分布**：36% 指向 x.com / 微信 / HuggingFace（抓不到）、
32% 与我们已有信源重复、**32% 是真正新增且可抓**（claude.com、blog.google、
blogs.nvidia.com、cursor.com、runwayml.com、dev.to、arcprize.org 等）。
微信公众号 3 篇实测全部返回 17.7KB 空壳页、正文提取长度 0，已进域名黑名单。

### 新增/改动的文件

```
新增  src/lib/news/aihot.ts                       线索解析（授权边界的代码落点）
新增  src/components/news/ai-notice-block.tsx     AI 标识说明条（三个列表页共用，避免漏）
新增  src/app/(shop)/news/archive/[month]/        按月归档页
新增  scripts/check-news-a1b1.ts                  50 条纯函数断言，不连库
新增  docs/AIHOT线索接入.md
改    prisma/schema.prisma                        6 项 DDL，全部可空 ADD COLUMN / CREATE INDEX
改    src/lib/news/{pipeline,format,score,feed,sources}.ts
改    src/lib/llm.ts                              记账三处修正
改    src/app/(shop)/news/{page,news-stream}.tsx、[slug]/page.tsx
改    src/app/api/news/list/route.ts              加 month / sort，返回体一个字段没减
改    src/app/api/admin/news/**                   积压统计、detail 编辑、AIHOT kind
改    src/app/sitemap.ts                          收录归档页
改    20 个前台页面 + globals.css                  A3
```

### 本轮刻意**没有**做的事

| 没做 | 为什么 |
|---|---|
| 超龄积压（45 天仍写不出摘要）的降级发布通道 | 会把 §8 的故障兜底变成常态化的逐字转载排程，是 SKILL §10 级别的合规姿态变更，不是常规迭代。需要你拍板 |
| 列表接口改游标分页 | offset 分页在几千行量级仍然够用（只是多扫几页索引）。上限从 50 页提到 500 页即可。真到需要游标的规模再换 |
| `BODY_CLIP` 2400 → 3200 | 收益不确定，却直接放大「正文未经分诊」的暴露面。先看 detail 的实际质量再说 |
| detail 回填默认开启 | 单价没核准之前不放开批量写作调用，见第十一节 |

---

## 十一、部署这一版（顺序不能改）

**不要用 `scripts/deploy.sh`**。它第 2/3 步 build+up、第 5 步才 `db push`，
顺序与「新增列」的要求正好相反：新镜像会在新列存在之前上线，
`/news/[slug]` 的 select 里带着还不存在的列 → 整页 500。
（详情页的 `getEvent` 已加 try/catch 兜到 404，但那是安全网不是方案。）

正确顺序是 **先 DDL、后 build**。六项变更全部是可空 `ADD COLUMN` 或 `CREATE INDEX`，
旧代码不读不写，所以「旧镜像还在跑的时候先建列」完全安全。

```bash
cd /opt/beiguo/beiguo-shop && set -a && . ./.env.production && set +a

# ① 备份并校验（半截 dump 比没有 dump 更危险）
docker compose --env-file .env.production exec -T db mysqldump -uroot -p"$MYSQL_ROOT_PASSWORD" \
  --single-transaction --routines --triggers --set-gtid-purged=OFF beiguo_shop \
  > /opt/beiguo/backups/beiguo_shop_$(date +%Y%m%d_%H%M%S).sql
tail -3 /opt/beiguo/backups/beiguo_shop_*.sql | grep "Dump completed" || echo "备份不完整，停手"

# ② 预览 DDL —— 出现任何 DROP 就停手
#    期望：news_events 加 4 列 + 2 索引；news_items 加 3 列
docker run --rm --network beiguo-shop_app-network -v "$PWD/prisma":/work/prisma -w /work \
  -e DATABASE_URL="mysql://root:$MYSQL_ROOT_PASSWORD@db:3306/beiguo_shop" \
  -e PRISMA_ENGINES_MIRROR=https://registry.npmmirror.com/-/binary/prisma \
  node:20-alpine sh -c 'apk add --no-cache openssl >/dev/null; npx -y prisma@5.22.0 migrate diff \
    --from-url "$DATABASE_URL" --to-schema-datamodel prisma/schema.prisma --script'

# ③ 先 db push（旧镜像仍在跑，对线上零影响）
docker run --rm --network beiguo-shop_app-network -v "$PWD/prisma":/work/prisma -w /work \
  -e DATABASE_URL="mysql://root:$MYSQL_ROOT_PASSWORD@db:3306/beiguo_shop" \
  -e PRISMA_ENGINES_MIRROR=https://registry.npmmirror.com/-/binary/prisma \
  node:20-alpine sh -c 'apk add --no-cache openssl >/dev/null; npx -y prisma@5.22.0 db push --skip-generate'
docker compose --env-file .env.production exec -T db mysql -uroot -p"$MYSQL_ROOT_PASSWORD" \
  -N -B beiguo_shop -e "SHOW COLUMNS FROM news_events LIKE 'base_score'; SHOW COLUMNS FROM news_items LIKE 'lead_via'"

# ④ 打回滚标签（rollback-f0cefe2 现在并不存在，必须现打）
docker tag beiguo-shop-app:latest beiguo-shop-app:rollback-f0cefe2

# ⑤ 腾空间（磁盘 90%）
#    ⚠️ 不要跑 docker system df 去「先看看有多少」—— 会把 dockerd 撑爆，见第六节第 3 条
docker builder prune -f
docker image prune -f

# ⑥ build + up（后台跑再轮询，别在 exec 会话里等）
nohup docker compose --env-file .env.production build app > /tmp/build.log 2>&1 &
#    另起一次 exec：
for i in $(seq 1 280); do pgrep -f "compose.*build" >/dev/null || break; sleep 3; done; tail -20 /tmp/build.log
docker compose --env-file .env.production up -d app

# ⑦ 冒烟
curl -s -o /dev/null -w "%{http_code}\n" -H "Host: bigolab.com" http://127.0.0.1/news
curl -s -o /dev/null -w "%{http_code}\n" -H "Host: bigolab.com" http://127.0.0.1/news/archive/2026-09
```

**⑥ 只 `up -d app`**，不要整栈 `up -d`：db / nginx / cloudflared 没有改动，
重建它们等于无谓地把站点断一下。app 是滚动替换，停机窗口只有容器重启那几秒。

### 上线后 24 小时要盯的四个数

1. `/admin/news` 的「待写摘要（积压）」—— 每小时应降 5 左右。连续几天不降要查
2. 「待补热度基准」—— rank 冷层每轮补 200，应该几轮归零。长期不降说明
   `RANK_SELECT` 漏了 `baseScore`（护栏失效，每轮在全量写 TEXT 列）
3. `docker compose logs app | grep '\[news/rank\]'` 的 `updated` —— 稳态应是个位数到几十。
   持续三位数就把 rank 的 crontab 改成每小时并回滚
4. 按北京日界统计的 `news_llm_calls` 花费 —— detail 让 compose 的输出 token 大约翻倍

---

## 十二、需要你拍板的事

| # | 问题 | 建议 |
|---|---|---|
| 1 | **智谱两个模型的实际单价**。`.env.production` 里 `LLM_IN_PRICE` / `LLM_OUT_PRICE` 是空的，走代码默认 `in=10 / out=100`（分/百万 token），而那是**写代码时的估算，不是核对过的报价** | 到智谱控制台查了填进去（官方标 X 元/百万 token 就填 X×100）。**不填准，`NEWS_DAILY_BUDGET_CENTS=300` 这道最后兜底就是纸糊的**。近 7 天账面只花 0.08 元，若单价填小 50 倍，真实是 4 元 |
| 2 | detail 老事件回填要不要开（`NEWS_DETAIL_BACKFILL=1`） | **等 1 确认后再开**。现存 93 条已发布事件没有 detail，开了约两三天补完 |
| 3 | 归档页用了 `/news/archive/2026-09` 而不是原计划的 `/news/2026-09` | 理由见第十节。要改回去也行，但正则必须锚定首尾并配断言脚本 |
| 4 | 超龄积压（45 天仍写不出摘要）怎么处置 | 本轮没做。先看双车道能不能消化掉。真要做「降级发布 + 标注」属合规姿态变更，得单独议 |
| 5 | AIHOT 授权邮件的原话：标注要求是「每一处展示都要标」还是「页面标注即可」？ | 当前是详情页标注 + 回链 + 信源列表逐条小标签，列表卡片不标。若要求每处都标，卡片布局要另改 |
| 6 | 磁盘 90%（40G 剩 4G） | 部署前那两条 prune 能腾出不少。长期要么扩盘要么定期清 |
