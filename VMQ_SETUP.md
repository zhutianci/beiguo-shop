# V免签式收款 接入说明（个人支付宝收款码 + 到账监控）

本项目已用「V免签」思路替换官方支付宝支付：展示你的个人支付宝收款码，买家按**系统分配的唯一金额**付款；一台常驻安卓手机上的监控 App 读取支付宝「收款到账」通知，推送到本站，按金额匹配订单后自动标记支付成功。

> ⚠️ 这是用个人收款码做经营性收款，属支付宝用户协议灰色地带，存在风控/冻结风险，请自行评估。仅个人小规模使用。

## 一、原理

1. 买家下单 → 系统在原价基础上分配一个**唯一金额**（被占用就 +0.01），并加锁。
2. 收银台展示你的收款码 + 该唯一金额，买家**精确支付**。
3. 监控 App 收到支付宝到账通知 → 调用本站 `/appPush` 上报金额。
4. 本站按 `金额 + 状态待支付` 匹配订单 → 标记已支付 → 自动履约（商品订单/发票税费）。
5. 超时未付的订单自动过期并释放金额。

## 二、服务端配置（.env.production）

```
VMQ_KEY=<与监控端一致的通讯密钥，openssl rand -hex 24>
VMQ_PAY_TIMEOUT=5          # 订单有效期(分钟)
VMQ_REQUIRE_MONITOR=1      # 1=监控端离线时禁止下单(防漏单)；0=不校验
```

改完执行 `docker compose up -d`（不是 restart）重建容器使其生效。

## 三、收款码图片

把你的支付宝「个人收款码」截图保存为 **`public/vmq-alipay-qr.png`**（随镜像构建进去，或挂载）。收银台会显示该图片。

## 四、监控端二选一

### 方式 B（推荐）：SmsForwarder 通知转发 → Webhook

[SmsForwarder](https://github.com/pppscn/SmsForwarder) 活跃维护、可监听应用通知并自定义 HTTP 请求。金额解析放在**服务端**（比老 APK 死文案宽松）。

1. 服务端设置 **`VMQ_REQUIRE_MONITOR=0`**（SmsForwarder 没有心跳；不设为 0 会被"监控端离线"拦截下单）。`VMQ_WEBHOOK_TOKEN` 留空则复用 `VMQ_KEY`。
2. 手机装 SmsForwarder → 「通知使用权」授权 → 允许读取支付宝通知。
3. 支付宝：开「支付助手 → 接收付款消息提醒」，保证到账消息进**系统通知栏**。
4. SmsForwarder 加一条「应用通知」转发规则（来源=支付宝），发送通道选 **Webhook**：
   - 请求地址：`https://bigolab.com/api/pay/sms-notify`
   - 请求方式：`POST`
   - 请求头：`Content-Type: application/json`
   - 请求体（WebParams，用 SmsForwarder 的 `[...]` 占位符）：
     ```json
     {"token":"<VMQ_KEY>","content":"[content]","from":"[from]","org":"[org_content]"}
     ```
   > `[content]`=通知内容(含金额)、`[from]`=来源、`[org_content]`=原始内容。后台「收款监控」页有现成可复制的地址/Body。
5. 用 App「测试」发一条，后台「收款监控 → 最近一次通知转发」应出现记录（含解析金额）。

> 服务端解析：从文案里优先取「收款/到账/¥…元」附近的金额，按 `金额+渠道` 匹配待支付订单。渠道含"微信"判为微信，否则支付宝。

### 方式 A：VmqApk（备选，7×24 在线）

1. 安装监控端：https://github.com/szvone/VmqApk （安卓真机或模拟器）。
2. **后台 →「收款监控」页** 会显示扫码配置二维码 + 监控端在线状态。
   - App 点「扫码配置」扫该二维码即可；
   - 或「手动配置」粘贴页面给出的「扫码内容」。
3. 开启「通知使用权 / 辅助服务」，并：
   - 支付宝 App：首页搜「支付助手」→ 开启「接收付款消息提醒」
   - 手机把 支付宝 + 监控端 都加入**内存清理白名单 / 无限制后台**，关省电优化
4. 配置成功后，后台「收款监控」页的「监控端」会变成「在线」（约 10 秒刷新）。

> **扫码二维码内容格式**（与 VmqApk 源码一致）：`host/key`
> - host = **纯域名**（如 `bigolab.com`，不带 `http://`、不带路径、可带端口）
> - key = 通讯密钥（= `VMQ_KEY`）
> - App 内部 `scanResult.split("/")` 取 `[0]=host、[1]=key`，并以 `http://host/appHeart` 发请求（OkHttp 会自动跟随 301 到 https）。
> 后台「收款监控」页已按此格式生成二维码，无需手工拼。

> 本站协议与官方 V免签 完全一致：
> - 心跳 `GET /appHeart?t=<ms>&sign=md5(t+key)`
> - 到账 `GET /appPush?t=<ms>&type=2&price=<金额>&sign=md5(type+price+t+key)`（type：1 微信 2 支付宝）

## 五、覆盖范围

- 商品下单支付：收银台 `/pay/<orderId>`
- 发票税费支付：同收银台
- 监控端离线时（60s 无心跳）下单会被拦截，提示稍后再试（可用 `VMQ_REQUIRE_MONITOR=0` 关闭）。

## 六、排障

- **一直不跳转“支付成功”**：监控端是否在线（看后台/数据库 `vmq_lastheart`）；支付宝到账提醒是否开启；金额是否与收银台**完全一致**。
- **下单提示“监控端离线”**：先在手机上把监控端跑起来并出现心跳。
- **金额对不上**：务必让买家付**收银台显示的金额**（可能比原价多几分，是为并发去重）。
