# V免签式收款 接入说明（个人支付宝收款码 + SmsForwarder 通知转发）

本项目已用「V免签」思路替换官方支付宝支付：展示你的个人支付宝收款码，买家按**系统分配的唯一金额**付款；一台常驻安卓手机上的 [SmsForwarder](https://github.com/pppscn/SmsForwarder) 监听支付宝「收款到账」通知并转发到本站 Webhook，服务端解析金额、按金额匹配订单后自动标记支付成功。

> ⚠️ 这是用个人收款码做经营性收款，属支付宝用户协议灰色地带，存在风控/冻结风险，请自行评估。仅个人小规模使用。

> 说明：旧版监控端 App（VmqApk，`/appHeart` + `/appPush` + 扫码 host/key + md5 签名 + 心跳在线判定）已从本项目**完全移除**，只保留 SmsForwarder 一条链路。历史部署若还配着 `VMQ_REQUIRE_MONITOR`，该变量现已无效，可直接删掉。

## 一、原理

1. 买家下单 → 系统在原价基础上分配一个**唯一金额**（被占用就 +0.01），并加锁。
2. 收银台展示你的收款码 + 该唯一金额，买家**精确支付**。
3. SmsForwarder 收到支付宝到账通知 → `POST /api/pay/sms-notify` 上报通知原文。
4. 服务端解析金额（见「四、金额识别规则」），按 `金额 + 渠道 + 状态待支付` 匹配订单 → 标记已支付 → 自动履约（商品订单/发票税费）。
5. 超时未付的订单自动过期并释放金额。

## 二、服务端配置（.env.production）

```
VMQ_KEY=<通讯密钥，openssl rand -hex 24>
VMQ_PAY_TIMEOUT=20         # 订单有效期(分钟)
VMQ_WEBHOOK_TOKEN=         # Webhook 校验 token，留空则复用 VMQ_KEY
```

改完执行 `docker compose up -d`（不是 restart）重建容器使其生效。

## 三、收款码图片

把你的支付宝「个人收款码」截图保存为 **`public/vmq-alipay-qr.png`**（随镜像构建进去，或挂载）。收银台会显示该图片。

## 四、金额识别规则（重要）

支付宝收钱码会发**两类**通知，只有一类代表「这一笔钱到账了」：

| 通知文案 | 处理 |
| --- | --- |
| `已转入余额 … 你已成功收款14.30元（老顾客消费）` | ✅ **取用**——这是本次实收金额 |
| `上一笔播报：支付宝到账 14.30 元。收钱提醒助手正在为您服务` | ❌ **主动拒绝**——播报的是上一笔的金额 |

规则细节：

- 只认强信号 **「你已成功收款 X 元」**（`你已成功收款 / 已成功收款 / 成功收款` + 紧邻金额），不对整串文本跑宽松正则。
  SmsForwarder 会把 `content`/`org_content`/`title`/`from` 拼成一长串，同一金额可能出现 2~3 次、不同来源文本混在一起，宽松匹配极易取错。
- 强信号前 24 字内出现「上一笔 / 上笔播报 / 历史播报 / 最近一笔」→ 判为播报回顾，跳过。
- 没有强信号、但整体是「到账 / 收钱提醒 / 上一笔」类文案 → 明确记为 `broadcast_rejected`（被规则拒绝），而不是「文案没覆盖到」。

未取用的原因会原样展示在后台「收款监控」页：

| reason | 中文含义 |
| --- | --- |
| `empty` | 空内容 |
| `broadcast_rejected` | 播报类通知（上一笔金额），已按规则拒绝 |
| `no_strong_signal` | 未出现「你已成功收款X元」强信号，未取用 |

> 被拒绝的通知会在后台用**黄色**高亮，便于一眼看出「有钱进来了但没被取用」；确认确实收到款后，用「补单」手动履约。

渠道判定：文案含「微信 / wechat / weixin / tenpay / 收款助手」判为微信，否则为支付宝。

## 五、SmsForwarder 配置步骤

1. 手机装 SmsForwarder → 授予「通知使用权」→ 允许读取支付宝通知。
2. 支付宝：开「支付助手 → 接收付款消息提醒」，保证到账消息进**系统通知栏**。
3. 把 支付宝 + SmsForwarder 加入**内存清理白名单 / 无限制后台**，关省电优化。
4. SmsForwarder 加一条「应用通知」转发规则（来源 = 支付宝），发送通道选 **Webhook**：
   - 请求地址：`https://bigolab.com/api/pay/sms-notify`
   - 请求方式：`POST`
   - 请求头：`Content-Type: application/json`
   - 请求体（WebParams，用 SmsForwarder 的 `[...]` 占位符）：
     ```json
     {"token":"<VMQ_KEY>","content":"[content]","from":"[from]","org":"[org_content]"}
     ```
   > `[content]`=通知内容(含金额)、`[from]`=来源、`[org_content]`=原始内容。
   > 后台「收款监控」页有现成可一键复制的地址 / token / Body。
5. 用 App「测试」发一条，后台「收款监控 → 最近一次通知转发」应出现记录（含解析金额或未取用原因）。

## 六、覆盖范围

- 商品下单支付：收银台 `/pay/<orderId>`
- 发票税费支付：同收银台
- SmsForwarder **没有心跳**，后台「最近一次收到转发」只是展示信息，**不会拦截下单**——任何时候都能下单支付。

## 七、排障

- **一直不跳转"支付成功"**：看后台「收款监控 → 最近一次通知转发」。
  - 空 → 通知没转发进来：SmsForwarder 通知使用权 / 规则是否命中支付宝 / Webhook 地址与 token 是否正确 / 支付宝支付助手提醒是否开启。
  - 有记录但显示「金额未取用」→ 看 reason（多为播报类通知被拒），确认收款后走「补单」。
  - 有金额但「未匹配」→ 买家付的金额与收银台**唯一金额**不一致，或订单已过期。
- **金额对不上**：务必让买家付**收银台显示的金额**（可能比原价多几分，是为并发去重）。
- **重复到账**：`vmqOrder` state 0→1 用条件更新原子翻转，同一笔被重复推送只会履约一次。
